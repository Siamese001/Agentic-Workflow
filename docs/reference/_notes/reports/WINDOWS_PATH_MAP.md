# Windows Path Map

This package has no top-level wrapper folder to reduce Windows Explorer path length risk.

## Folder shift
- `C0_Context_Engine/` is now `03A_C0_Context_Engine/`.
- `PA_Prompt_Assembly/` is now `03B_PA_Prompt_Assembly/`.

## Reason
These are route-adjacent surfaces selected by L0, not children owned by L0.
