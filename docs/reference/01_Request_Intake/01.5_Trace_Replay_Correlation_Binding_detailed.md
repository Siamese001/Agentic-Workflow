========================================================================================================================
WINDSURF IMPLEMENTATION CONTRACT
01.5 TRACE / REPLAY / CORRELATION BINDING
========================================================================================================================

OVERWRITE RECONCILIATION HEADER
Canonical filename: 01.5_Trace_Replay_Correlation_Binding_detailed.md
Overwrite mode: full-file, no-overlap, implementation-grade contract
Parent: 01_Request_Intake_detailed.md

UNIQUE OWNERSHIP SURFACE:
- Request identifiers, trace root, normalized request hash, and ingress replay seed only.

THIS FILE OWNS:
- RequestCorrelationReceipt.
- RequestId / SessionId / TraceRoot binding.
- IngressReplaySeed.
- NormalizedRequestHash.
- IntakeManifestHash.
- Provisional span reconciliation.
- Correlation rejection reasons.

THIS FILE DOES NOT OWN:
- Route digest.
- Prompt hash.
- Evidence contract hash.
- L2 attempt seed.
- Provider request IDs.
- Completed-run replay comparison.
- L6 RCA or learning replay analysis.

GLOBAL NO-OVERLAP LOCK:
- U0 / Intake owns raw ingress, transport envelope, caller/session baseline, quota/duplicate checks, structural schema validation, correlation IDs, trace root, normalized payload handoff, and intake rejection receipts only.
- L1 owns semantic intent interpretation, task_spec, query_spec, ambiguity register, plan contract, and proposed route recommendations.
- L0 owns route selection, RouteContract, grounding_required, execution_form, fallback_chain, and route authority.
- C0 owns retrieval planning, fetching, graph expansion, shaping, final evidence scoring, FinalEvidenceContract, and weak-support refinement.
- Prompt Assembly owns authority-tiered prompt slots, U0 task slot placement, retrieved-content packing, tool/schema binding, and signed provider-ready prompt artifacts.
- L3 owns managed workflow expansion, step dependencies, joins, retries, pause/resume shape, and L3StepContract.
- L2 owns bounded execution, tool/model/script invocation inside capability and sandbox, repair, and sealed work artifacts.
- Runtime Gates and Exit Eval own current-run dispositions and final checkout decisions.
- L5 owns governance certification evidence, origin-trust certification, authority context, provider/egress certification, HITL re-clearance evidence, and replay/audit certification evidence.
- L4/UWG owns durable write admission and system-of-record mutation.
- L6 owns completed-run evaluation, RCA, learning proposals, replay proving, and future-run promotion only.

FORBIDDEN OUTPUTS FROM U0 / INTAKE FILES:
- L1PlanContract
- RouteContract
- RetrievalPlan
- FinalEvidenceContract
- PromptEnvelope
- CompiledPromptArtifact
- L3WorkflowContract
- L3StepContract
- L2ExecutionRequest
- SealedL2Artifact
- ExitReviewPacket
- ExitDisposition
- GateVerdict as final runtime disposition
- CommitRequest
- UWGCommitReceipt
- ShadowEvalRecord
- LearningProposal
- ALLOW
- DENY
- CLARIFY
- ABSTAIN
- REROUTE
- SHRINK_SCOPE
- RETRY
- HEAL
- ESCALATE_HITL
- QUARANTINE
- REDACT
- SAFE_FALLBACK
- MARK_DEGRADED
- COMMIT_REQUEST
- BLOCK_COMMIT
- ALLOW_FINISH
- approve_execution
- approve_output
- approve_write

ALLOWED OUTPUT STYLE:
- intake receipts
- validation manifests
- normalized request envelopes
- caller/session/tenant baselines
- quota and duplicate receipts
- trace/correlation receipts
- rejected_request packets
- validated_request packets
- hashes, evidence refs, reason codes, and non-authoritative hints only

SPEC LENGTH NOTE:
- This file is intentionally detailed.
- Detail is valid only where it defines this file's unique Intake surface.
- Do not compress away fields, receipts, gates, traces, tests, or acceptance criteria.

END OVERWRITE RECONCILIATION HEADER
========================================================================================================================


OBJECTIVE
------------------------------------------------------------------------------------------------------------------------
Implement 01.5 as the deterministic correlation and replay seed stage.

01.5 must answer one question only:
"Can this normalized intake packet be stamped with stable identity, trace, and replay bindings before L1 receives it?"

01.5 creates the root proof surface for the run. It does not know the route, prompt, evidence, or execution attempt yet.

SOURCE SPEC TO FOLLOW:
- Use 01_request_intake_detailed.md as the parent authority for U0 / Intake doctrine and handoff shape.
- Use agentic_system_process_map_exec.md as authoritative for U0 position before L1.
- Use 02_L1_Reasoning_Plan_Generation_detailed.md as authoritative for what happens after intake handoff.
- Use Evaluation_Runtime_Gates_detailed.md as authoritative for G01-G04 gate vocabulary and runtime dispositions.
- Use 00_L5_Governance_Safety_detailed.md and its children as authoritative for governance certification evidence.
- Use 03_L0_Route_Decision_Switching_L3_detailed.md, C0_Context_Engine_detailed.md, Prompt_Assembly_detailed.md, 04_L2_Execute_detailed.md, 05_Live_Runtime_Exit_Control_&_Evaluation_detailed.md, and 06_Shadow_Evaluation_System_Learning_detailed.md as downstream boundaries that Intake must not absorb.

Hard rule: Intake files may reference those files, but must not restate their implementation detail or claim their authority.

IMPLEMENTATION PRINCIPLE:
Build the smallest real runtime surface that proves this Intake stage works end-to-end.

Do not create decorative abstractions.
Do not create mock-only code that cannot be wired into runtime.
Do not satisfy this with comments or placeholder functions.
Do not invent a parallel intake path.
Do not move downstream responsibilities into this child.
Do not introduce broad refactors unrelated to this Intake contract.

The implementation must be executable, testable, traceable, replayable, and observable through deterministic receipts.


EXPECTED TARGET AREAS
------------------------------------------------------------------------------------------------------------------------
Preferred logical module layout:

src/
  intake/
    contracts.py
    correlation.py
    trace_binding.py
    intake_replay.py
    manifest_hash.py
    otel.py

tests/
  intake/
    test_01_5_correlation_contracts.py
    test_01_5_trace_binding.py
    test_01_5_replay_hashes.py
    test_01_5_otel_trace.py
    test_01_5_negative_boundaries.py

PHASE 0 DISCOVERY REQUIREMENT

Before coding, inspect the repo for existing:
- request / API / CLI / UI ingress handlers
- request envelope models
- auth, session, tenant, ACL, and identity utilities
- quota / throttling / rate-limit utilities
- schema validation framework
- sanitization / origin-label / security finding utilities
- OTEL tracing helpers
- replay/hash helpers
- existing U0 / intake tests and fixtures
- handoff code into L1
- bypasses that allow L1/L0/C0/PA/L2 to receive raw user input directly

Produce a short implementation plan before editing code.

The plan must identify:
- files to modify
- files to create
- contracts to reuse
- tests to add
- unresolved ambiguity
- any boundary where another source-owned file must be changed instead

No broad refactors.
No unrelated renames.
No public API changes unless required for U0 / Intake compatibility.


PHASE 1 DATA CONTRACTS
------------------------------------------------------------------------------------------------------------------------
1. RequestCorrelationReceipt

Fields:
- receipt_id
- request_id
- session_id
- trace_root
- tenant_id
- principal_id_hash
- raw_envelope_id
- normalized_payload_id
- correlation_source
- parent_trace_ref optional
- provisional_span_refs[]
- deterministic_receipt_hash

Validation:
- request_id required.
- session_id required.
- trace_root required.
- tenant_id required unless anonymous_limited policy allows none.
- deterministic hash excludes observed_at and wall-clock volatile fields.

2. IngressReplaySeed

Fields:
- replay_seed_id
- request_id
- session_id
- tenant_id
- principal_id_hash
- transport
- raw_payload_hash
- normalized_payload_hash
- schema_version
- intake_policy_ref
- replay_key_seed
- replay_seed_hash

Validation:
- replay_key_seed is not RouteContract replay_key.
- replay_seed_hash deterministic from stable input fields.

3. NormalizedRequestHash

Fields:
- hash_id
- normalized_payload_hash
- caller_scope_baseline_hash
- schema_version
- origin_label_manifest_hash
- entry_policy_refs[]
- normalized_request_hash

Rules:
- Exclude volatile timing and random IDs.
- Include tenant/session boundary.
- Include schema and origin label manifests.
- Include raw payload hash by reference.

4. IntakeManifestHash

Fields:
- manifest_hash_id
- transport_receipt_hash
- caller_scope_baseline_hash
- quota_receipt_hash
- schema_validation_receipt_hash
- origin_label_manifest_hash
- normalized_request_hash
- replay_seed_hash
- intake_manifest_hash

Validation:
- child receipt hashes required.
- intake_manifest_hash deterministic and stable across replay for same logical input.

PHASE 2 PIPELINE
------------------------------------------------------------------------------------------------------------------------
Public entrypoint:

bind_trace_and_replay(schema_normalization_result, correlation_policy) -> TraceReplayBindingResult

Required steps:
1. Bind or create request_id.
2. Confirm session_id from 01.2.
3. Create trace_root or reconcile provisional span context.
4. Compute normalized_request_hash.
5. Compute ingress replay seed.
6. Compute intake manifest hash over child receipts.
7. Emit RequestCorrelationReceipt.
8. Attach correlation fields to handoff candidate.

Hard no:
- No route_digest.
- No prompt_hash.
- No evidence_contract_hash.
- No execution attempt seed.
- No mutation to L4.
- No completed-run replay comparison.

PHASE 3 OTEL
------------------------------------------------------------------------------------------------------------------------
Required span:
- name: u0.intake.trace_replay_binding
- attributes:
  - intake.stage = 01.5
  - request_id
  - session_id
  - trace_root
  - normalized_request_hash_prefix
  - intake_manifest_hash_prefix
  - replay_seed_hash_prefix
  - receipt_id

PHASE 4 TESTS
------------------------------------------------------------------------------------------------------------------------
Required tests:
- assigns request_id when absent.
- preserves session_id from identity baseline.
- creates trace_root.
- reconciles provisional transport span.
- same logical input produces same normalized_request_hash.
- volatile observed_at does not change intake_manifest_hash.
- tenant/session changes do change normalized_request_hash.
- proves no route_digest or prompt_hash is emitted.

IMPLEMENTATION ACCEPTANCE CRITERIA
------------------------------------------------------------------------------------------------------------------------
A. Real runtime behavior
- The stage has a public entrypoint that can be invoked by the Intake pipeline.
- The stage returns typed receipts, not loose dictionaries, unless the repo's existing schema system requires dictionaries.
- The stage fails closed when required fields are absent.
- The stage emits deterministic hashes for replay-relevant receipts.
- The stage emits OTEL spans with request_id and trace_root when those fields are available.

B. Boundary proof
- Tests prove this stage does not call L1 planning.
- Tests prove this stage does not call L0 routing.
- Tests prove this stage does not call C0 retrieval.
- Tests prove this stage does not call Prompt Assembly.
- Tests prove this stage does not call L2 execution.
- Tests prove this stage does not write to L4 or UWG.
- Tests prove this stage does not emit ExitDisposition.

C. Negative tests
- Malformed input is rejected with a typed reason.
- Missing mandatory metadata fails closed.
- Volatile fields do not pollute deterministic manifest hashes.
- Raw payload is preserved separately from normalized payload.
- The failure path still emits enough audit evidence to diagnose the rejection.

FINAL WINDSURF RESPONSE REQUIREMENT
------------------------------------------------------------------------------------------------------------------------
When implementation is complete, report:
- Files changed.
- Tests added.
- Tests run and exact command output.
- One sample request through U0 showing every Intake child stage receipt.
- OTEL trace IDs / span IDs for the sample run.
- Replay evidence showing the same input produces the same deterministic intake manifest.
- Explicit statement of any skipped item with reason.

