# Proposed pyproject.toml changes (v4 — runtime-scan-derived core)
# Core = runtime scan hard imports only (excl scripts/dashboards)

--- a/pyproject.toml
+++ b/pyproject.toml

## [project].dependencies (core)

 dependencies = [
-    "pytest>=7.4.0",                # → [dev]
-    "pytest-cov>=4.1.0",             # → [dev]
-    "pytest-asyncio>=0.21.0",        # → [dev]
-    "google-genai>=1.0.0",           # → [infra] (deferred/conditional)
-    "pinecone-client>=3.0.0",        # REMOVE (renamed)
-    "redis>=5.0.0",                  # stays or → [infra] per scan
-    "cryptography>=41.0.0",          # → [infra] (no direct import)
+    "PyYAML>=6.0",                   # 1 runtime hard
+    "aiofiles>=23.0.0",              # 3 runtime hard
+    "chromadb>=0.4.0",               # 1 runtime hard
+    "duckdb>=0.9.0",                 # 1 runtime hard
+    "jinja2>=3.1.0",                 # 1 runtime hard
     "libcst>=1.1.0",
+    "networkx>=3.0",                 # 3 runtime hard
+    "numpy>=1.24.0",                 # 9 runtime hard
+    "pinecone>=5.0.0",               # 1 runtime hard
+    "psutil>=5.9.0",                 # 1 runtime hard
     "pydantic>=2.0.0",
+    "pydantic-settings>=2.0.0",      # 1 runtime hard
+    "python-dotenv>=1.0.0",          # 1 runtime hard
+    "rank-bm25>=0.2.0",              # 2 runtime hard
+    "redis>=5.0.0",                  # 4 runtime hard
+    "scikit-learn>=1.3.0",           # 1 runtime hard
+    "tenacity>=8.2.0",               # 1 runtime hard
+    "tqdm>=4.65.0",                  # 1 runtime hard
+    "watchdog>=3.0.0",               # 1 runtime hard
 ]

## [project.optional-dependencies]

+dev = [
+    "pytest>=7.4.0",
+]
+infra = [
+    "FlagEmbedding>=1.0.0",
+    "GitPython>=3.1.0",
+    "PyPDF2>=3.0.0",
+    "anthropic>=0.20.0",
+    "bandit>=1.7.0",
+    "beautifulsoup4>=4.12.0",
+    "boto3>=1.28.0",
+    "dash>=2.14.0",
+    "fastapi>=0.100.0",
+    "google-genai>=1.0.0",
+    "google-generativeai>=0.3.0",
+    "livereload>=2.6.0",
+    "neo4j>=5.0.0",
+    "openai>=1.0.0",
+    "opentelemetry-api>=1.20.0",
+    "pandas>=2.0.0",
+    "pdf2image>=1.16.0",
+    "pdfplumber>=0.10.0",
+    "playwright>=1.40.0",
+    "plotly>=5.18.0",
+    "pypdf>=3.0.0",
+    "pytesseract>=0.3.10",
+    "pytz>=2023.3",
+    "requests>=2.28.0",
+    "rich>=13.0.0",
+    "sentence-transformers>=2.2.0",
+    "tabulate>=0.9.0",
+    "tiktoken>=0.5.0",
+    "torch>=2.0.0",
+    "tree-sitter>=0.20.0",
+    "tree-sitter-python>=0.20.0",
+    "uvicorn>=0.23.0",
+    "waitress>=2.1.0",
+    "websockets>=11.0.0",
+]
+sdks = [
+    "backoff>=2.2.0",
+    "google-cloud-aiplatform>=1.38.0",
+    "jsonschema>=4.20.0",
+]

# pip install -e .              # core (19 pkgs)
# pip install -e '.[dev]'       # + dev (1 pkgs)
# pip install -e '.[infra]'     # + infra (34 pkgs)
# pip install -e '.[sdks]'      # + sdks (3 pkgs)
# pip install -e '.[dev,infra]'  # full dev environment
