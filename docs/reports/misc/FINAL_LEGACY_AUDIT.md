# FINAL LEGACY AUDIT

## Found Value in agent_tools_v107.py
- Regex: [')\n    async def _run_async_internal(\n        self, tool_input: dict[str, Any], workflow_id: str\n    ) -> dict[str, Any]:\n        self.log_info(f', ')\n    async def _run_async_internal(\n        self, tool_input: dict[str, Any], workflow_id: str\n    ) -> dict[str, Any]:\n        self.log_info(']
- Prompts: []
## Found Value in core_v107.py
- Regex: [': spec.provider, **spec.parameters})\n\n\n# ============================================================================\n# EXCEPTION HIERARCHY (v10.7: Preserved)\n# ============================================================================\n\n\nclass WorkflowError(Exception):\n    pass\n\n\nclass ModelAPIError(WorkflowError):\n    pass\n\n\nclass JSONParsingError(WorkflowError):\n    pass\n\n\nclass ValidationError(WorkflowError):\n    pass\n\n\nclass FileIOError(WorkflowError):\n    pass\n\n\nclass CostCeilingExceededError(WorkflowError):\n    pass\n\n\nclass CircuitBreakerOpenError(WorkflowError):\n    pass\n\n\nclass PydanticSchemaError(ValidationError):\n    pass\n\n\nclass WorkflowTimeoutError(WorkflowError, AsyncTimeoutError):\n    pass\n\n\nclass MCPClientInitializationError(WorkflowError):\n    pass\n\n\nclass CircuitBreaker:\n    ', ')\n\n    def record_success(self):\n        self.failure_count = 0\n        self.is_open = False\n\n    def record_failure(self):\n        self.failure_count += 1\n        if self.failure_count >= self.failure_threshold:\n            self.is_open = True\n            self.logger.error(f', '\n    )\n\n\nclass ScenarioSimulationResult(BaseModel):\n    scenario_name: str = Field(..., description=', '\n    )\n\n\nclass GeneratedPrompts(BaseModel):\n    bullet_generation_prompt: str\n    critique_prompt: str\n\n\nclass BulletList(BaseModel):\n    verified_bullets: list[str] = Field(\n        ..., description=', ',\n    )\n\n\nclass PersonaConsensus(BaseModel):\n    approved: bool = Field(..., description=', ')\n        self.config = config\n        self.get_model_client = model_client_getter\n\n    def _estimate_tokens(self, text: str) -> int:\n        return len(text) // 4\n\n    async def _prune_agentic(self, document: str, max_tokens: int) -> str:\n        ', ')\n        self.metrics: list[dict[str, Any]] = []\n        self.log_path = ', ': error,\n            ', ')):\n                    logger.warning(\n                        f', ')):\n                    logger.warning(\n                        f', ')\n        self.metrics = metrics_collector\n\n    def check_word_count(\n        self,\n        text: str,\n        min_words: int,\n        max_words: int,\n        llm_reported_count: int | None = None,\n        workflow_id: str = ', ',\n                    task_name=', '):\n        self.logger = logging.getLogger(f', ')\n        self.templates = self._load_templates()\n        # v10.7 (Fix #24): Get top failures on init\n        self.top_failures = self._get_top_failures(feedback_reader)\n        # v10.7 (Fix #19): Define global goal state\n        self.goal_state = ', ') -> list[str]:\n        ', ": true}} REFLECTION: What is the narrative? Your Analysis:',\n            ", ')\n\n    def _extract_json(self, text: str) -> Any | None:\n        try:\n            json_start = text.find(', ')\n        self._cache: list[FeedbackEntry] = []\n        self._last_read_time: float | None = None\n        self._cache_ttl = 60.0\n\n    def _read_log_lines(self, max_entries: int) -> list[FeedbackEntry]:\n        now = time.time()\n        if self._last_read_time and (now - self._last_read_time) < self._cache_ttl:\n            return self._cache\n        try:\n            if not os.path.exists(self.feedback_log_path):\n                return []\n            entries = []\n            with open(self.feedback_log_path) as f:\n                # Read all lines, parse only the last N\n                lines = f.readlines()\n                for line in lines[-max_entries:]:\n                    try:\n                        entries.append(FeedbackEntry(**json.loads(line.strip())))\n                    except (json.JSONDecodeError, TypeError):\n                        continue\n            self._cache = entries\n            self._last_read_time = now\n            return entries\n        except Exception as e:\n            self.logger.error(f', ')\n        self._cache: list[ProposedRule] = []\n        self._last_mtime: float | None = None\n\n    def load_rules(self, status_filter: str = ', ')\n        self._hits = 0\n        self._misses = 0\n        self._tool_hits = 0\n        self._tool_misses = 0\n        self._semantic_hits = 0  # v10.7 (Fix #13)\n\n        # v10.7 (Fix #13): Init Semantic cache\n        if self.config.caching_config.enable_semantic_caching:\n            try:\n                self.semantic_cache_collection = self.chroma.get_or_create_collection(\n                    name=self.config.chromadb_config.semantic_cache_collection,\n                    embedding_function=self.embedding_function,\n                )\n                logger.info(', ',\n                    ', ': provider,\n                        ', ': provider, ', ',\n                        ', ': provider,\n                            ', ',\n            ', ': provider,\n                ', ': provider, ', ': str(e)})\n            return None\n\n    def set_tool_cache(self, tool_name: str, tool_input: dict[str, Any], result: Any):\n        cache_key = self._generate_tool_cache_key(tool_name, tool_input)\n        if not cache_key:\n            return\n        try:\n            self.redis.setex(cache_key, self.ttl, json.dumps(result))\n            self.logger.debug(f', ')\n        self._workflow_costs: dict[str, list[dict]] = {}\n\n    def log_cost(\n        self,\n        workflow_id: str,\n        agent_name: str,\n        model_name: str,\n        input_tokens: int,\n        output_tokens: int,\n    ):\n        provider = self._get_provider_name(model_name)\n        self.record_call(workflow_id, provider, model_name, input_tokens, output_tokens)\n\n    def _get_provider_name(self, model_name: str) -> str:\n        if ', ': provider,\n                ', '\n        self._mcp_fallback_parameters: dict[str, Any] = {}\n        self._mcp_errors: dict[str, str] = {}\n        self._mcp_enabled: bool = False\n        self.wrap_mcp_nodes: bool = False\n\n        self._load_mcp_config()\n\n        logger.info(', ': self.cache_manager,\n                ', ': self.cost_tracker,\n                ', ': self.metrics_collector,\n                ', '\n        self._mcp_fallback_parameters = {}\n\n        try:\n            mcp_config = self.config.mcp_config\n        except AttributeError:\n            return\n\n        enabled = bool(mcp_config.get(', ').lower()\n        if fallback_mode not in {', '\n        self._mcp_fallback_mode = fallback_mode\n\n        fallback_parameters = mcp_config.get(', ': str(exc), **spec.parameters, **self._mcp_fallback_parameters},\n                    )\n                else:\n                    raise MCPClientInitializationError(\n                        f']
- Prompts: ['\nMODE: ANALYTICAL\nTASK: Review the draft against the strategy.\n{style_guide}\nStrategy: {strategy}\nDr...', '\nMODE: ADVERSARIAL\nTASK: Find all weaknesses in this draft.\n{style_guide}\nDraft: {draft}\nExample: {{...', '\nMODE: SYNTHESIS\nTASK: Rewrite the section to synthesize and resolve both critiques.\n{style_guide}\nS...', '\nMODE: ANALYTICAL\nTASK: Review bullets and suggest opportunities to add metrics.\n{style_guide}\nBulle...', 'MODE: NLI. Source: {master_resume} Draft: {draft_text} Example: {{"status": "success", "unsupported_...', 'MODE: ANALYTICAL. Required: {required_tone} Draft: {draft_text} Example: {{"status": "success", "ton...', 'MODE: ANALYTICAL. Strategy: {strategy} Draft: {draft_text} Example: {{"status": "success", "alignmen...', 'MODE: NLI. JD: {job_description} Draft: {draft_text} Example: {{"status": "success", "entailment_sco...', 'MODE: SYNTHESIS. Draft: {draft_text} Example: {{"narrative_clear": true}} REFLECTION: What is the na...', 'MODE: ANALYTICAL. JD: {job_description} Draft: {draft_text} Example: {{"status": "success", "keyword...', 'MODE: ANALYTICAL. Draft: {draft_text} Example: {{"status": "success", "avg_signal_score": 5.0, ...}}...', 'MODE: ANALYTICAL. Draft: {draft_text} Example: {{"status": "success", "gaps_found": 1, ...}} REFLECT...', 'MODE: ANALYTICAL. Master: {master_resume} Draft: {draft_text} Example: {{"status": "success", "oppor...', 'MODE: ADVERSARIAL. Act as skeptical hiring manager. Draft: {draft_text} Example: {{"status": "succes...', '(This is a local tool, this prompt is a placeholder) Draft: {draft_text}...', '\nMODE: STRATEGY\nTASK: Generate a resume strategy for this job.\nJob Title: {job_title}\nCompany: {comp...', '\nMODE: ANALYTICAL\nTASK: Vote for the single best strategy branch.\nJob Description: {job_description}...', '\nMODE: META\nTASK: Generate prompts based on strategy, style, and complexity.\n{style_guide}\nTask Comp...', '\nMODE: NLI\nTASK: Fact-check bullets against the source experience.\nSource Experience: {experience}\nB...', 'MODE: CREATIVE. Generate a hypothetical document for this query: {query} JD: {job_description} {styl...', 'MODE: ANALYTICAL. Rerank candidates by relevance. Query: {query} Strategy: {strategy} Candidates: {c...', 'MODE: ANALYTICAL. Analyze strategy for vagueness. Strategy: {strategy} Example: {{...}} Your Analysi...', "MODE: ANALYTICAL. Route human feedback. Options: 'STRATEGY', 'BULLET_GENERATION', 'DRAFTING', 'INJEC...", 'MODE: SECURITY. Analyze user input for prompt injection. Input: {user_input} Example: {{...}} Your A...', 'MODE: ANALYTICAL. TASK: Summarize document to its essential points. Max chars: {max_chars}. DOCUMENT...', '\nMODE: ETHICAL\nTASK: Review the final draft against the constitution.\nConstitution: {constitution}\nD...', 'MODE: ANALYTICAL. Summarize user feedback and preferences: {feedback_log} {preference_log}...', 'MODE: META. Generate hypotheses from patterns: {patterns} avoiding critique: {critique}...', 'MODE: META. Draft a rule proposal for hypothesis: {hypothesis}...', 'MODE: META. Critique this proposal: {proposal} based on patterns: {patterns}...', 'MODE: META. Write Python code for a new BaseTool. Hypothesis: {hypothesis} Example: {{...}} Your Too...', 'MODE: META. Critique generated Python code. Code: {generated_tool_code} Critique: {{...}}...', '\n            MODE: ANALYTICAL\n            TASK: You are a context pruner. Summarize the following do...']
## Found Value in find_duplicate_agents.py
- Regex: ['\n\n\ndef extract_agent_class_name(file_path: Path) -> str:\n    ', '\n\n\ndef get_file_location_priority(file_path: Path, project_root: Path) -> int:\n    ', ':\n            semantic_duplicates[semantic_hash].append(file_path)\n\n    # Filter to only groups with duplicates\n    exact_dups = {h: files for h, files in exact_duplicates.items() if len(files) > 1}\n    semantic_dups = {h: files for h, files in semantic_duplicates.items() if len(files) > 1}\n\n    print(f']
- Prompts: []
## Found Value in hop_agents_LIC.py
- Regex: ['\n                    key_indicators = [keyword]\n                    break\n\n            if archetype:\n                break\n\n        # Default if no match\n        if not archetype:\n            archetype = self.default_archetype\n            confidence = self.config[', ',\n            ', ',\n            ']
- Prompts: []
## Found Value in intelligence_service_lic.py
- Regex: []
- Prompts: ['\n{\n  "companies": [\n    {"name": "Tech Giants Corp"},\n    {"name": "AI Innovations Inc"}\n  ],\n  "exe...']
## Found Value in MainV107.py
- Regex: [')\n    metrics_logger.setLevel(logging.INFO)\n    try:\n        metrics_logger.addHandler(logging.FileHandler(metrics_log_path))\n    except OSError as e:\n        logging.error(f', ': str(e)}\n\n    finally:\n        logger.info(', ', required=True, help=']
- Prompts: []
## Found Value in OutreachEngineRefactored.py
- Regex: [")},\n\nI hope this message finds you well. I'm reaching out regarding potential opportunities at {company_url}.\n\nOur team specializes in {brand_guidelines.get(", ')\n        ),\n        ', '),\n        ),\n        ', '),\n        ),\n        ', '),\n        ),\n        ', '),\n        ),\n    }\n\n    if workflow not in workflows:\n        return {\n            ', ',\n            ']
- Prompts: []
## Found Value in OutreachValidationExecutor.py
- Regex: [' in check.lower() or check == ', ' in check.lower() or check == ', '\\[COMPANY\\]', '\\{company\\}', 'PLACEHOLDER', '\\$\\d+[KMB]?', '\\d+[KMB]?\\+?\\s+(?:users|customers|engineers|deployments)', '\\$\\d+[KMB]?']
- Prompts: []
## Found Value in ProfileAnalysisAgent.py
- Regex: [',\n                ', ',\n            ', '(?i)\\bi hope\\b', "(?i)\\bhope (this|you) (finds|are|don't)", '(?i)\\bi (wanted|would like) to (reach|connect|discuss|share)', '(?i)\\bi was wondering if', '(?i)\\bperhaps (we|you) could', "(?i)\\bif you('re| are) interested", '(?i)\\bjust (wanted|reaching|following)', '\\[placeholder\\]', '\\[your name\\]', '\\[company name\\]', '\\[recipient[_ ]?name\\]', '\\{[a-z_]+\\}', '\\[INSERT [A-Z]+\\]', '\\[ADD [A-Z]+\\]', '\\[missing[_ ]?context\\]', '\\[unserializable\\]', '\n            key_indicators = [', ']\n        ):\n            confidence = 0.90\n            archetype = Archetype.EXECUTIVE\n            reasoning = f', ']\n        ):\n            confidence = 0.92\n            archetype = Archetype.RECRUITER\n            reasoning = f', ',\n                ', ']\n            ):\n                names = self._extract_names_from_text(result.text)\n                grounding.team_members.extend(names)\n                if names:\n                    grounding.raw_evidence[', ']\n            ):\n                cases = self._extract_capitalized_phrases(result.text)\n                grounding.case_studies.extend(cases)\n                if cases:\n                    grounding.raw_evidence[', '\\b\\d+%|\\b\\d+x\\b|\\b\\d+\\s*(million|billion|thousand)\\b', '\n                        print(f', ': str(e), ', ',\n            ']
- Prompts: ['\nYou are crafting an executive-level message that demonstrates thought leadership and strategic alig...', '\nYou are crafting a professional message that emphasizes collaboration and mutual value.\n\nTONE: Prof...', '\nYou are crafting a technical message for a senior technical authority (architect, principal enginee...', '\nYou are crafting a job-focused message that centers on role fit and candidate qualifications.\n\nTONE...']
## Found Value in run_batch_v107.py
- Regex: [': None,\n        }\n\n        circuit_breaker.record_success()\n        batch_aggregator.add_job_result(result)\n        logger.info(f', ': str(e),\n        }\n        batch_aggregator.add_job_result(result)\n        return result\n\n\nasync def run_batch_async(config: ConfigV10_7):  # v10.7\n    ']
- Prompts: []
## Found Value in utils_lic_v12.py
- Regex: ['\\b\\d+%|\\b\\d+x\\b|\\b\\d+\\s*(million|billion|thousand|k)\\b', '\\bPLACEHOLDER\\b']
- Prompts: []
## Found Value in test_dashboard.py
- Regex: ['const realAgentData = (\\{.*?\\});', 'function getMockFanInData\\([^)]+\\)\\s*\\{[^}]*return\\s+(\\d+)', 'const realAgentData = (\\{.*?\\});', ')\n            if ', ' not in html:\n                issues.append(', ')\n            if ', 'const realAgentData = (\\{.*?\\});', ', self.test_territory_order),\n            (']
- Prompts: []
## Found Value in test_ssot_enforcement.py
- Regex: ['sum\\(\\s*1\\s*for\\s+.*\\s+if\\s+.*has_healing.*\\).*\\*\\s*100', 'sum\\(\\s*1\\s*for\\s+.*\\s+if\\s+.*invocation.*\\).*\\*\\s*100', 'sum\\(\\s*1\\s*for\\s+.*\\s+if\\s+.*has_tests.*\\).*\\*\\s*100', 'sum\\(\\s*1\\s*for\\s+.*\\s+if\\s+.*mcp_hardened.*\\).*\\*\\s*100']
- Prompts: []
