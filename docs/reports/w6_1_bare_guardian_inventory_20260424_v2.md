# W6.1 BARE-Guardian Inventory v2 (Corrected) — 2026-04-24

> Supersedes `docs/reports/w6_1_bare_guardian_inventory_20260424.md`. The v1
> report used a too-strict regex (`#\s*guardian`) that missed the canonical
> `# noqa: BLE001 -- guardian: allow-<type>` pattern used throughout the
> codebase. The corrected regex is `guardian\s*:\s*allow-\S+`.

## Headline

The originally-deferred "1696 sites across L5/L0/L3" figure is **stale by
an even larger margin than first thought**:

- **Total bare/broad except handlers: 1,313** (not 1,696)
- **Already guardian-covered: 450 (34%)**
- **Uncovered: 863 (66%)** — all in tooling layers
- **Production layers (L0–L6, L_APP, L_INFRA, L_SL, other): 100% guardian-covered**
- **Actionable production scope after this session: 0 sites** ✅

## Method

1. AST-scan `agentic_core`, `apps_*`, `tools`, `ops_scripts`, `system_learning`, `infrastructure` for `ExceptHandler` nodes with `type=None | Name('Exception') | Name('BaseException')`
2. For each, check lines ±1 for `guardian: allow-<type>` token
3. Classify by layer using file-path prefix

Tooling: `@c:/Git/Agentic-Workflow/tools/debug/_bare_guardian_coverage.py`

## Layer-by-Layer Coverage (post-fix)

| Layer | Covered | Uncovered | Total | Coverage % | Priority |
|---|---:|---:|---:|---:|:---:|
| **L0** | 0 | 0 | 0 | n/a | — |
| **L1** | 4 | 0 | 4 | 100% | ✅ |
| **L2** | 8 | 0 | 8 | 100% | ✅ |
| **L3** | 22 | 0 | 22 | 100% | ✅ |
| **L4** | 2 | 0 | 2 | 100% | ✅ |
| **L5** | 49 | 0 | 49 | 100% | ✅ |
| **L6** | 0 | 0 | 0 | n/a | — |
| **L_APP** | 73 | 0 | 73 | 100% | ✅ (closed this session) |
| **L_INFRA** | 1 | 0 | 1 | 100% | ✅ |
| **L_OPS** | 119 | 76 | 195 | 61% | 🟡 Tooling — future |
| **L_SL** | 3 | 0 | 3 | 100% | ✅ |
| **L_TOOLS** | 140 | 787 | 927 | 15% | 🟢 Tooling — codemod candidate |
| **other** | 29 | 0 | 29 | 100% | ✅ |

## Fix Applied This Session

`@c:/Git/Agentic-Workflow/apps_eval/integrations/tracing.py:159` — the last
uncovered production site. The OTel span-recorder's `except BaseException`
catch is intentional (re-raises unchanged after `span.record_exception`) but
lacked the explicit guardian comment. Added:

```
except BaseException as exc:  # guardian: allow-broad-exception --
    OTel span exception recorder; catches ALL (incl
    KeyboardInterrupt/SystemExit) to call span.record_exception then
    re-raises unchanged on line 168; re-raise preserves original control flow
```

## Remaining Scope (863 tooling sites)

All in `tools/` (787) and `ops_scripts/` (76). These are offline,
non-production scripts where broad exception handling is often legitimate:

- Migration codemods (catch-all to ensure source is not corrupted on failure)
- CI gate runners (catch-all to emit a clean failure report)
- Debug scanners (catch-all to continue scanning past one bad file)

These don't carry production runtime risk. A codemod pass to add guardian
tokens is **straightforward but low-value** — they'd mostly say
`# guardian: allow-broad-exception -- offline tooling, reports failure`.

## Recommendation

**Close W6.1 as production-complete.** Queue the remaining 863 tooling sites
as a separate lower-priority wave (`W6.1-Tooling-Codemod`) that can run
unattended via a single codemod pass. No dedicated multi-session effort
required.

## Comparison to Prior Estimates

| Estimate | Claim | Reality |
|---|---|---|
| Original deferred-scope | 1,696 sites L5/L0/L3 phased, multi-session | Zero uncovered L5/L0/L3 — those are done |
| v1 inventory (this session) | 45 L_APP production sites | 0 uncovered L_APP (regex false-negative) |
| v2 inventory (final) | 0 production sites uncovered | ✅ closed |

Same pattern as SC-1 (54→3) and W6.3-P1 (34→0): **stale probe × prior
remediation progress = over-estimated backlog**. W6.1 production is
effectively done.

---

_Generated by `tools/debug/_bare_guardian_coverage.py` — 2026-04-24._
