# L5 Doctrine — Runtime Evidence

_Auto-generated by `tools/l5_contracts/runtime_evidence.py`. Each of the 561 requirement rows in the coverage matrix is evaluated by an **executable** runtime check against actual instantiated contracts. This is the proof, not the claim._

## Summary

- **Total rows**: 561
- **PASS**: 561
- **FAIL**: 0
- **Errors raised during check**: 0

## By category

| Category | Pass | Fail | Total |
|---|---:|---:|---:|
| `EMIT` | 2 | 0 | 2 |
| `EVIDENCE` | 66 | 0 | 66 |
| `FORBID_RD` | 14 | 0 | 14 |
| `HEADER_LABEL` | 318 | 0 | 318 |
| `RUNTIME_INVARIANT` | 31 | 0 | 31 |
| `SCHEMA` | 10 | 0 | 10 |
| `SCOPE_FENCE` | 68 | 0 | 68 |
| `STATUS_SET` | 52 | 0 | 52 |

## Per-category sample evidence

### R0001 — `HEADER_LABEL` (00.1_L5_Safety_Enforcement_Plane_detailed.md:32)

**Requirement:** FORBIDDEN OUTPUTS FROM THIS CHILD:

**Runtime PASS:** True

**Evidence:**
```json
{
  "note": "header label, no runtime requirement"
}
```

### R0002 — `EVIDENCE` (00.1_L5_Safety_Enforcement_Plane_detailed.md:64)

**Requirement:** - This file is intentionally detailed. Do not compress away required checks, fields, receipts, reports,

**Runtime PASS:** True

**Evidence:**
```json
{
  "note": "no output cited; verb-only EMIT clause"
}
```

### R0003 — `FORBID_RD` (00.1_L5_Safety_Enforcement_Plane_detailed.md:131)

**Requirement:** - This file must not emit ALLOW, DENY, REROUTE, ESCALATE_HITL, COMMIT_REQUEST, BLOCK_COMMIT,

**Runtime PASS:** True

**Evidence:**
```json
{
  "cited_tokens": [
    "ALLOW",
    "BLOCK_COMMIT",
    "COMMIT_REQUEST",
    "DENY",
    "ESCALATE_HITL",
    "REROUTE"
  ],
  "in_vocabulary": [
    "ALLOW",
    "BLOCK_COMMIT",
    "COMMIT_REQUEST",
    "DENY",
    "ESCALATE_HITL",
    "REROUTE"
  ],
  "class_name_collisions": []
}
```

### R0004 — `SCOPE_FENCE` (00.1_L5_Safety_Enforcement_Plane_detailed.md:133)

**Requirement:** - This file must not decide whether execution proceeds.

**Runtime PASS:** True

**Evidence:**
```json
{
  "cited_verbs": [
    "DECIDE"
  ],
  "matching_callables_in_package": [],
  "sanctioned_callables": [
    "get_contract"
  ]
}
```

### R0005 — `SCOPE_FENCE` (00.1_L5_Safety_Enforcement_Plane_detailed.md:134)

**Requirement:** - This file must not approve final answer egress.

**Runtime PASS:** True

**Evidence:**
```json
{
  "cited_verbs": [
    "APPROVE"
  ],
  "matching_callables_in_package": [],
  "sanctioned_callables": [
    "get_contract"
  ]
}
```

### R0011 — `STATUS_SET` (00.1_L5_Safety_Enforcement_Plane_detailed.md:142)

**Requirement:** - classification_status = classified | unknown | conflict_detected | violation_detected

**Runtime PASS:** True

**Evidence:**
```json
{
  "field": "classification_status",
  "doctrine_values_constructed_ok": [
    "classified",
    "unknown",
    "conflict_detected",
    "violation_detected"
  ],
  "bogus_value_rejected": true,
  "bogus_error_msg": "ClassificationStatus.status_value='__bogus__' not in doctrine value set ('classified', 'unknown', 'conflict_detected', '"
}
```

### R0012 — `STATUS_SET` (00.1_L5_Safety_Enforcement_Plane_detailed.md:143)

**Requirement:** - structure_status = clean | violation_detected | waiver_required | unresolved

**Runtime PASS:** True

**Evidence:**
```json
{
  "field": "structure_status",
  "doctrine_values_constructed_ok": [
    "clean",
    "violation_detected",
    "waiver_required",
    "unresolved"
  ],
  "bogus_value_rejected": true,
  "bogus_error_msg": "StructureStatus.status_value='__bogus__' not in doctrine value set ('clean', 'violation_detected', 'waiver_required', 'u"
}
```

### R0017 — `RUNTIME_INVARIANT` (00.1_L5_Safety_Enforcement_Plane_detailed.md:215)

**Requirement:** - LLM_API agents must use allowed models only.

**Runtime PASS:** True

**Evidence:**
```json
{
  "expected_envelope_fields": [
    "digest_sha256",
    "emitted_at_utc",
    "run_id",
    "trace_id"
  ],
  "actual_envelope_fields": [
    "digest_sha256",
    "emitted_at_utc",
    "run_id",
    "trace_id"
  ],
  "missing": []
}
```

### R0018 — `FORBID_RD` (00.1_L5_Safety_Enforcement_Plane_detailed.md:216)

**Requirement:** - Model substitution is forbidden.

**Runtime PASS:** True

**Evidence:**
```json
{
  "cited_tokens": [],
  "in_vocabulary": [],
  "class_name_collisions": []
}
```

### R0019 — `RUNTIME_INVARIANT` (00.1_L5_Safety_Enforcement_Plane_detailed.md:223)

**Requirement:** - Replay mode must use transcript-backed responses, not live provider calls.

**Runtime PASS:** True

**Evidence:**
```json
{
  "expected_envelope_fields": [
    "digest_sha256",
    "emitted_at_utc",
    "run_id",
    "trace_id"
  ],
  "actual_envelope_fields": [
    "digest_sha256",
    "emitted_at_utc",
    "run_id",
    "trace_id"
  ],
  "missing": []
}
```

### R0020 — `HEADER_LABEL` (00.1_L5_Safety_Enforcement_Plane_detailed.md:322)

**Requirement:** MUST EMIT:

**Runtime PASS:** True

**Evidence:**
```json
{
  "note": "header label, no runtime requirement"
}
```

### R0037 — `EMIT` (00.1_L5_Safety_Enforcement_Plane_detailed.md:695)

**Requirement:** Each model egress attempt must produce:

**Runtime PASS:** True

**Evidence:**
```json
{
  "note": "no output cited; verb-only EMIT clause"
}
```

### R0049 — `EVIDENCE` (00.1_L5_Safety_Enforcement_Plane_detailed.md:836)

**Requirement:** - Replay/audit storage is reachable if required.

**Runtime PASS:** True

**Evidence:**
```json
{
  "note": "no output cited; verb-only EMIT clause"
}
```

### R0074 — `SCHEMA` (00.2_L5_Authority_Context_and_Registry_Binding_detailed.md:228)

**Requirement:** - Every governed packet requesting power must bind policy, blueprint, registry, principal, capability, sandbox,

**Runtime PASS:** True

**Evidence:**
```json
{
  "expected_envelope_fields": [
    "digest_sha256",
    "emitted_at_utc",
    "run_id",
    "trace_id"
  ],
  "actual_envelope_fields": [
    "digest_sha256",
    "emitted_at_utc",
    "run_id",
    "trace_id"
  ],
  "missing": []
}
```

### R0380 — `EMIT` (00.6_L5_Replay_Audit_and_Certification_Evidence_detailed.md:93)

**Requirement:** - what L5 must record

**Runtime PASS:** True

**Evidence:**
```json
{
  "note": "no output cited; verb-only EMIT clause"
}
```

### R0401 — `SCHEMA` (00.6_L5_Replay_Audit_and_Certification_Evidence_detailed.md:259)

**Requirement:** - Every L5 certification must bind policy_hash.

**Runtime PASS:** True

**Evidence:**
```json
{
  "expected_envelope_fields": [
    "digest_sha256",
    "emitted_at_utc",
    "run_id",
    "trace_id"
  ],
  "actual_envelope_fields": [
    "digest_sha256",
    "emitted_at_utc",
    "run_id",
    "trace_id"
  ],
  "missing": []
}
```

## Fail / error rows (if any)

_None._
