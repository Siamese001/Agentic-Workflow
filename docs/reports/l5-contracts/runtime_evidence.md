# L5 Doctrine — Runtime Evidence

_Auto-generated by `tools/l5_contracts/runtime_evidence.py`. Each of the 575 requirement rows in the coverage matrix is evaluated by an **executable** runtime check against actual instantiated contracts. This is the proof, not the claim._

## Summary

- **Total rows**: 575
- **PASS**: 575
- **FAIL**: 0
- **Errors raised during check**: 0

## By category

| Category | Pass | Fail | Total |
|---|---:|---:|---:|
| `EMIT` | 2 | 0 | 2 |
| `EVIDENCE` | 67 | 0 | 67 |
| `FORBID_RD` | 23 | 0 | 23 |
| `HEADER_LABEL` | 318 | 0 | 318 |
| `RUNTIME_INVARIANT` | 31 | 0 | 31 |
| `SCHEMA` | 11 | 0 | 11 |
| `SCOPE_FENCE` | 69 | 0 | 69 |
| `STATUS_SET` | 54 | 0 | 54 |

## Per-category sample evidence

### R0001 — `FORBID_RD` (00A.1_L5_Safety_Enforcement_Plane_detailed.md:52)

**Requirement:** Forbidden ownership in this 00A pack:

**Runtime PASS:** True

**Evidence:**
```json
{
  "cited_tokens": [],
  "in_vocabulary": [],
  "class_name_collisions": []
}
```

### R0002 — `HEADER_LABEL` (00A.1_L5_Safety_Enforcement_Plane_detailed.md:92)

**Requirement:** FORBIDDEN OUTPUTS FROM THIS CHILD:

**Runtime PASS:** True

**Evidence:**
```json
{
  "note": "header label, no runtime requirement"
}
```

### R0003 — `EVIDENCE` (00A.1_L5_Safety_Enforcement_Plane_detailed.md:124)

**Requirement:** - This file is intentionally detailed. Do not compress away required checks, fields, receipts, reports,

**Runtime PASS:** True

**Evidence:**
```json
{
  "note": "no output cited; verb-only EMIT clause"
}
```

### R0004 — `FORBID_RD` (00A.1_L5_Safety_Enforcement_Plane_detailed.md:191)

**Requirement:** - This file must not emit ALLOW, DENY, REROUTE, ESCALATE_HITL, COMMIT_REQUEST, BLOCK_COMMIT,

**Runtime PASS:** True

**Evidence:**
```json
{
  "cited_tokens": [
    "ALLOW",
    "BLOCK_COMMIT",
    "COMMIT_REQUEST",
    "DENY",
    "ESCALATE_HITL",
    "REROUTE"
  ],
  "in_vocabulary": [
    "ALLOW",
    "BLOCK_COMMIT",
    "COMMIT_REQUEST",
    "DENY",
    "ESCALATE_HITL",
    "REROUTE"
  ],
  "class_name_collisions": []
}
```

### R0005 — `SCOPE_FENCE` (00A.1_L5_Safety_Enforcement_Plane_detailed.md:193)

**Requirement:** - This file must not decide whether execution proceeds.

**Runtime PASS:** True

**Evidence:**
```json
{
  "cited_verbs": [
    "DECIDE"
  ],
  "matching_callables_in_package": [],
  "sanctioned_callables": [
    "get_contract"
  ]
}
```

### R0006 — `SCOPE_FENCE` (00A.1_L5_Safety_Enforcement_Plane_detailed.md:194)

**Requirement:** - This file must not approve final answer egress.

**Runtime PASS:** True

**Evidence:**
```json
{
  "cited_verbs": [
    "APPROVE"
  ],
  "matching_callables_in_package": [],
  "sanctioned_callables": [
    "get_contract"
  ]
}
```

### R0012 — `STATUS_SET` (00A.1_L5_Safety_Enforcement_Plane_detailed.md:202)

**Requirement:** - classification_status = classified | unknown | conflict_detected | violation_detected

**Runtime PASS:** True

**Evidence:**
```json
{
  "field": "classification_status",
  "doctrine_values_constructed_ok": [
    "classified",
    "unknown",
    "conflict_detected",
    "violation_detected"
  ],
  "bogus_value_rejected": true,
  "bogus_error_msg": "ClassificationStatus.status_value='__bogus__' not in doctrine value set ('classified', 'unknown', 'conflict_detected', '"
}
```

### R0013 — `STATUS_SET` (00A.1_L5_Safety_Enforcement_Plane_detailed.md:203)

**Requirement:** - structure_status = clean | violation_detected | waiver_required | unresolved

**Runtime PASS:** True

**Evidence:**
```json
{
  "field": "structure_status",
  "doctrine_values_constructed_ok": [
    "clean",
    "violation_detected",
    "waiver_required",
    "unresolved"
  ],
  "bogus_value_rejected": true,
  "bogus_error_msg": "StructureStatus.status_value='__bogus__' not in doctrine value set ('clean', 'violation_detected', 'waiver_required', 'u"
}
```

### R0018 — `RUNTIME_INVARIANT` (00A.1_L5_Safety_Enforcement_Plane_detailed.md:275)

**Requirement:** - LLM_API agents must use allowed models only.

**Runtime PASS:** True

**Evidence:**
```json
{
  "expected_envelope_fields": [
    "digest_sha256",
    "emitted_at_utc",
    "run_id",
    "trace_id"
  ],
  "actual_envelope_fields": [
    "digest_sha256",
    "emitted_at_utc",
    "run_id",
    "trace_id"
  ],
  "missing": []
}
```

### R0020 — `RUNTIME_INVARIANT` (00A.1_L5_Safety_Enforcement_Plane_detailed.md:283)

**Requirement:** - Replay mode must use transcript-backed responses, not live provider calls.

**Runtime PASS:** True

**Evidence:**
```json
{
  "expected_envelope_fields": [
    "digest_sha256",
    "emitted_at_utc",
    "run_id",
    "trace_id"
  ],
  "actual_envelope_fields": [
    "digest_sha256",
    "emitted_at_utc",
    "run_id",
    "trace_id"
  ],
  "missing": []
}
```

### R0021 — `HEADER_LABEL` (00A.1_L5_Safety_Enforcement_Plane_detailed.md:382)

**Requirement:** MUST EMIT:

**Runtime PASS:** True

**Evidence:**
```json
{
  "note": "header label, no runtime requirement"
}
```

### R0038 — `EMIT` (00A.1_L5_Safety_Enforcement_Plane_detailed.md:755)

**Requirement:** Each model egress attempt must produce:

**Runtime PASS:** True

**Evidence:**
```json
{
  "note": "no output cited; verb-only EMIT clause"
}
```

### R0050 — `EVIDENCE` (00A.1_L5_Safety_Enforcement_Plane_detailed.md:896)

**Requirement:** - Replay/audit storage is reachable if required.

**Runtime PASS:** True

**Evidence:**
```json
{
  "note": "no output cited; verb-only EMIT clause"
}
```

### R0076 — `SCHEMA` (00A.2_L5_Authority_Context_and_Registry_Binding_detailed.md:288)

**Requirement:** - Every governed packet requesting power must bind policy, blueprint, registry, principal, capability, sandbox,

**Runtime PASS:** True

**Evidence:**
```json
{
  "expected_envelope_fields": [
    "digest_sha256",
    "emitted_at_utc",
    "run_id",
    "trace_id"
  ],
  "actual_envelope_fields": [
    "digest_sha256",
    "emitted_at_utc",
    "run_id",
    "trace_id"
  ],
  "missing": []
}
```

### R0386 — `EMIT` (00A.6_L5_Replay_Audit_and_Certification_Evidence_detailed.md:153)

**Requirement:** - what L5 must record

**Runtime PASS:** True

**Evidence:**
```json
{
  "note": "no output cited; verb-only EMIT clause"
}
```

### R0407 — `SCHEMA` (00A.6_L5_Replay_Audit_and_Certification_Evidence_detailed.md:319)

**Requirement:** - Every L5 certification must bind policy_hash.

**Runtime PASS:** True

**Evidence:**
```json
{
  "expected_envelope_fields": [
    "digest_sha256",
    "emitted_at_utc",
    "run_id",
    "trace_id"
  ],
  "actual_envelope_fields": [
    "digest_sha256",
    "emitted_at_utc",
    "run_id",
    "trace_id"
  ],
  "missing": []
}
```

## Fail / error rows (if any)

_None._
