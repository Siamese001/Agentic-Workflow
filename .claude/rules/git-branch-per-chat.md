# Named Worktree Isolation — stub

> On-demand (historical filename retained). **Invariant:** durable work belongs in explicitly chosen registered sibling worktrees, normally on IDE-owned `codex/<topic>` or `claude/<topic>` branches cut from `origin/main`, with matching C:/Git directory names like `Agentic-Workflow-FRESH-codex-<topic>` or `Agentic-Workflow-FRESH-claude-<topic>`; never edit the primary checkout while it is on `main`/`master`. `work/<topic>` and `feat/<topic>` are legacy durable names; `chat/<timestamp>` is legacy ephemeral and should not be used for new work. SessionStart is advisory only: no automatic per-chat worktree creation and no cleanup. Detail: [`worktree-per-chat`](../skills/worktree-per-chat/SKILL.md). Enforced: `before_file_edit_branch_guard.py`. Manual cleanup/report: `prune_merged_chat_worktrees.py --dry-run`, then `--delete-merged` only by explicit operator action. Bypass: `WORKTREE_PER_CHAT_BYPASS=1`.
