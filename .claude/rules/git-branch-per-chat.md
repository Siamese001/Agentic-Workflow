# Named Worktree Isolation — stub

> On-demand (historical filename retained). **Invariant:** durable editable work belongs in explicitly chosen registered sibling worktrees on agent-owned, high-signal local branches named `codex-<scope>` or `claude-<scope>` cut from `origin/main`; the worktree folder basename must exactly equal the local branch name (for example `C:/Git/Agentic-Workflow-FRESH-worktrees/codex-worktree-naming-contract` on branch `codex-worktree-naming-contract`). Never edit the primary checkout while it is on `main`/`master`. Slash namespaces (`codex/<topic>`, `claude/<topic>`, `work/<topic>`, `feat/<topic>`) are legacy and must be migrated before further edits; `chat/<timestamp>` is legacy ephemeral and should not be used for new work. SessionStart is advisory only: no automatic per-chat worktree creation and no cleanup. Detail: [`worktree-per-chat`](../skills/worktree-per-chat/SKILL.md). Enforced: `before_file_edit_branch_guard.py`. Manual cleanup/report: `prune_merged_chat_worktrees.py --dry-run`, then `--delete-merged` only by explicit operator action. Bypass: `WORKTREE_PER_CHAT_BYPASS=1`.
