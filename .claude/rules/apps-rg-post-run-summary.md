# apps_rg Post-Run Summary — stub

> On-demand under apps_rg (plan `always-on-rule-surface-cut-c7f3a1`); enforcement unchanged. After every Claude-orchestrated apps_rg run, render `tools/apps_rg/render_run_summary.py` markdown inline, verbatim — it is the artifact ground truth behind the §37 verdict (layers on the response floor, never replaces it). Detail: [`apps-rg-runtime`](../skills/apps-rg-runtime/SKILL.md) skill. Bypass: `APPS_RG_SUMMARY_BYPASS=1`.
