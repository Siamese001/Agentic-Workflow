# ADG Post-Run Burndown — stub

> On-demand during ADG work (plan `always-on-rule-surface-cut-c7f3a1`); enforcement unchanged. A `generate_full_adg` / `adg_gates/run.py` run is not PASS unless the SAME response renders the burndown-by-severity table + per-gate (FIX/TRACK) table inline (source: `artifacts/adg/adg_burndown_report.md`). FIX-first; TRACK never mass-fixed. Detail: [`adg-sqlite`](../skills/adg-sqlite/SKILL.md) skill. Enforced: `post_agent_adg_burndown_inline_audit.py`. Bypass: `ADG_BURNDOWN_INLINE_BYPASS=1`.
