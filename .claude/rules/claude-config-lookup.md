# Claude Code Config Lookup — stub

> On-demand for config-lookup questions (plan `always-on-rule-surface-cut-c7f3a1`). For Claude Code behavior / this repo's `.claude` config, search local-first: CLAUDE.md → `.claude/rules/*.md` → `.claude/skills/**/SKILL.md` → `.claude/agents/*.md` → `.claude/settings.json` + `.claude/hooks/**` → `.mcp.json` + `.claude/mcp-notes.md` → `.claude/commands/*.md`. `.cursor/`/`.windsurf/` are legacy (the one live dependency is `.claude/governance/scripts/**`).
