# Legacy Cursor hooks (archived W1)

| Hook | Superseded by |
|------|----------------|
| `after_agent_author_gate_audits.py` | `after_agent_governance_dispatch.py` |

Not registered in `hooks.json`. Tests that asserted chain order now target `after_agent_governance_dispatch.py`.
