# Python environment requirements for Agentic-Workflow
# Install all dependencies with:
#    pip install -r requirements.txt

pydantic
pytest
pytest-asyncio
httpx
requests
python-dotenv
rich
numpy
pandas
tqdm

# Optional: only if your retrieval/ranking stack uses them
scikit-learn
sentence-transformers

# Optional: only if Golden Eval or RAG hybrid uses them
faiss-cpu

# For observability & testing
opentelemetry-api
opentelemetry-sdk

# For runtime utilities
tenacity

# For Neo4j graph database support
neo4j>=5.22.0
