# apps_eval Legacy Quarantine

This directory preserves the pre-reset `apps_eval` implementation for audit
and migration reference.

Active `apps_eval` is now the deterministic grader harness for only
`apps_rg` and `apps_lic`. Do not import code from `apps_eval_legacy` in the
active harness.

