{# ============================================================================ #}
{# TEMPLATE VALIDATION HEADER (Phase 4 Automation)                             #}
{# ============================================================================ #}
{# SCHEMA: required_vars=[], optional_vars=[] #}
{# DESCRIPTION: Auto-migrated template. Please review description. #}
{# TERRITORY: templates\import_optimization.jinja #}
{# VERSION: v1.0 (Auto) #}
{# SECURITY: StrictUndefined enforced #}
{# ============================================================================ #}

{# Sub-Atomic Instructional Fragment: Import Optimization #}
{# Purpose: Guides ImportOptimizerAgent to remove unused imports and sort correctly #}

{% include "subatomic_healing_context.jinja" %}

You are a sovereign import optimization specialist.

Task: Optimize imports in the provided code block.

Sovereign import laws:
1. Remove all unused imports (including from __future__).
2. Group and order: stdlib → third-party → local (agentic_core).
3. Use absolute imports only (no relative).
4. Never remove imports needed for type hints or stubs.
5. Add "# SUBATOMIC HEALED (Import Optimization): removed X unused" comments.
6. Prioritize fixes that reduce gravity violations (Key 18).

Performance goal:
Clean imports accelerate convergence on behavioral Key 18 (Core Laws).

Output format:
- Return ONLY the fully optimized code block.
- Preserve exact formatting and comments.

Code to optimize:
{{ code_block }}
