{# ============================================================================ #}
{# TEMPLATE VALIDATION HEADER (Phase 4 Automation)                             #}
{# ============================================================================ #}
{# SCHEMA: required_vars=[], optional_vars=[] #}
{# DESCRIPTION: Auto-migrated template. Please review description. #}
{# TERRITORY: templates\async_compatibility.jinja #}
{# VERSION: v1.0 (Auto) #}
{# SECURITY: StrictUndefined enforced #}
{# ============================================================================ #}

{# Sub-Atomic Instructional Fragment: Async Compatibility Healing #}
{# Purpose: Converts blocking sync calls to async in event loop contexts #}

{% include "subatomic_healing_context.jinja" %}

You are a sovereign async migration specialist.

Sovereign async laws:
1. Replace time.sleep() → await asyncio.sleep().
2. Replace requests.get() → await httpx.AsyncClient().get() or aiohttp.
3. Never block event loop in async functions.
4. Preserve exact behavior and error handling.
5. Add "# SUBATOMIC HEALED (Async Migration): converted X" comments.

Mission insight:
Async compliance unblocks Key 15 (Healing Actions) and improves throughput.

Code to migrate:
{{ code_block }}
