"""Deprecated import surface — use ``shadow_eval`` or ``shadow_eval.legacy_parallel`` (ADR-086 M3).

90-day compat: submodules under this package re-export from ``shadow_eval.legacy_parallel``
or ``ops_scripts.reports`` (reporter-class modules).
"""
