"""Tier 6 Prompt B reference modules (static metadata).

Two modules, two contracts:
  * must_release_blocking_refs   — 6 MUST / RELEASE_BLOCKING rows
  * reference_only_policy_refs   — 15 NON_BLOCKING_REFERENCE rows

Pure metadata. No runtime, no OTEL, no tools, no state mutation.
"""
