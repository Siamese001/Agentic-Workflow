"""Auto-generated flattened view for orchestration/test_orchestration_state_isolation.py.

Do not edit this file directly; run tools/flatten_tests.py instead."""
from __future__ import annotations

import sys
import types
from pathlib import Path

REPO_ROOT = Path(__file__).resolve().parents[1]
SOURCE_ROOT = REPO_ROOT / "tests"
OUTPUT_ROOT = REPO_ROOT / "tests_flat"
SOURCE_REL_PATH = "orchestration/test_orchestration_state_isolation.py"
SOURCE_PATH = REPO_ROOT / SOURCE_REL_PATH

# Embedded canonical source
EMBEDDED_SOURCES = {
    SOURCE_REL_PATH: '''"""Orchestration-level regression tests for state mutation guarantees."""

from __future__ import annotations

import copy

import pytest

from agent_orchestration_v10_7 import run_inject_hil_edit
from core_v10_7 import MainGraphState


@pytest.mark.asyncio
async def test_run_inject_hil_edit_keeps_input_state_immutable(workflow_context):
    typed_state = MainGraphState()
    typed_state.metadata.workflow_id = "wf-immutability"
    typed_state.draft.sections = {"summary": {"draft": "Original summary"}}
    state = typed_state.to_dict()
    state.setdefault("hil", {})["payload"] = "Add new summary"

    baseline = copy.deepcopy(state)
    updated = await run_inject_hil_edit(state, workflow_context)

    assert state == baseline, "Orchestration nodes must not mutate the incoming state"
    assert (
        updated["draft"]["sections"]["summary"]["draft"].startswith("[EDITED BY HUMAN]")
    )
'''
}

ORIGINAL_MODULE_NAME = "tests.orchestration.test_orchestration_state_isolation"
FLAT_MODULE_NAME = "tests_flat.orchestration__test_orchestration_state_isolation"


def _ensure_import_roots() -> None:
    root = str(REPO_ROOT)
    flat_root = str(OUTPUT_ROOT)
    for candidate in (root, flat_root):
        if candidate not in sys.path:
            sys.path.insert(0, candidate)
    tests_pkg = sys.modules.setdefault("tests", types.ModuleType("tests"))
    tests_pkg.__path__ = list({*(getattr(tests_pkg, "__path__", []) or []), str(SOURCE_ROOT), flat_root})
    tests_flat_pkg = sys.modules.setdefault("tests_flat", types.ModuleType("tests_flat"))
    tests_flat_pkg.__path__ = list({*(getattr(tests_flat_pkg, "__path__", []) or []), flat_root})


def _materialize_module() -> types.ModuleType:
    module = types.ModuleType(ORIGINAL_MODULE_NAME)
    module.__file__ = str(SOURCE_PATH)
    module.__package__ = ORIGINAL_MODULE_NAME.rpartition(".")[0]
    exec(compile(EMBEDDED_SOURCES[SOURCE_REL_PATH], str(SOURCE_PATH), "exec"), module.__dict__)
    sys.modules[ORIGINAL_MODULE_NAME] = module
    sys.modules[FLAT_MODULE_NAME] = module
    return module


_ensure_import_roots()
_embedded_module = _materialize_module()
for _name, _value in list(_embedded_module.__dict__.items()):
    if _name == "__builtins__":
        continue
    globals()[_name] = _value
