# Ingestion pipeline — embedding implementation (00A) — pointer only

**SSOT:** `docs/reference/_notes/Ingestion Pipeline/00A_ingestion_embedding_implementation.md`

This file under `tools/reference/` is **not** maintained. Full content lives in the `_notes` path above.

If you landed here from an old link or bookmark, open the SSOT file instead.
