"""tools.proof — runtime-proof tooling.

Currently exposes ``req_compiler`` (Install 7 from apps_proof RCA): walks
``docs/reference/`` per-layer directories, extracts MUST / SHALL
requirements, and produces a runtime-evidence matrix that gates release.
"""
