name: skill-status-dashboard
description: Dashboard for monitoring skill health and performance
version: 1.0.0
parameters:
  format:
    type: string
    description: Output format (json, table, markdown)
  skill_filter:
    type: string
    description: Filter for specific skills (optional)
entrypoint: main.py
