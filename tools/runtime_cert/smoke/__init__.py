"""Runtime certification live-trace smoke harnesses (Phase C.6+).

Non-promoting, app-scoped smoke harnesses that wire the Phase C.1→C.2→C.3
pipeline end-to-end on a real runtime-ADG snapshot. These harnesses produce
structured evidence reports only — they never certify an app.
"""
