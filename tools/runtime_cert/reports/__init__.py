"""Runtime-certification reporting extractors (Phase C.7+).

Non-promoting report extractors that transform ``NormalizedTraceRow`` rows
and ``AppRouteContract`` contracts into human-actionable structured
reports. None of these modules certify an app — they produce gap /
readiness / backlog signals for operator review.
"""
