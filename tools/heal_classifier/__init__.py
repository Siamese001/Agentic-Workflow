"""Offline training pipeline for the C3 Heal Confidence Classifier v1.

Usage:
    python -m tools.heal_classifier train --dataset <path> --output-dir <path>
    python -m tools.heal_classifier report --artifact-dir <path> --packet-dir <path>
    python -m tools.heal_classifier verify --packet-dir <path>
"""
