# Windsurf rules mirror (legacy — read-only)

**Edit SSOT:** `.cursor/rules/*.mdc` only.

This directory mirrors historical Windsurf-era `.md` rules for CI parity and `trigger: always_on` budget reporting. Do **not** author new governance here.

- Cursor Tier-1 measurement: `.cursor/rules` (`alwaysApply: true`) + `AGENTS.md`
- Windsurf `always_on` bytes: reported separately by `ops_scripts/ci/check_always_on_token_budget.py`
- Migration map: `.cursor/MIGRATION_MAP.md`
- Plan: `.cursor/plans/cursor-governance-two-tier-b4e8f2.md`
