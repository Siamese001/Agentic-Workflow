from __future__ import annotations

"""Exposes the shared resume, job, and results models so every layer interprets candidate data the same way, leading to fairer evaluation and cleaner, more consistent formatting."""

from models import *  # noqa: F401,F403
