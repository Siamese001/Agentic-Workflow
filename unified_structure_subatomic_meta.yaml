structure_version: '1.1'
description: Single Source of Truth for Agentic Workflow Architecture
binds_to: unified_structure_subatomic.yaml
canonical_role: "Defines vocab constraints: domains, layers, phases, intents, axes, verb_groups, protected_paths, engines"

# Add protected path rules if missing
protected_paths:
  - "**/__init__.py"
  - "**/*.md"

domains:
  apps: Unified applications domain for user-facing engines (LIC + RG).
  agentic_core: Core reasoning, decision making, and autonomous logic engine.
  shared: Cross-domain shared utilities, base classes, and common logic.
  config: Configuration management, registry settings, and environment variables.
  data: Data persistence, retrieval, storage management, and database connectors.
  observability: System monitoring, logging, metrics collection, and diagnostics.
  prompt_governance: Management of prompt templates, versioning, and safety checks.
  runtime: Runtime execution environment, job scheduling, and process management.
  schemas: Data validation schemas, type definitions, and contract enforcement.
  scripts: Maintenance scripts, automation tools, and deployment helpers.
  tests: Test suites, integration tests, and validation frameworks.

engines:
  apps_rg: Resume Generation application engine (RG).
  apps_lic: LinkedIn Outreach application engine (LIC).

layers:
  L1_cognition: High-level understanding, planning, and intent recognition.
  L2_execution: Concrete action execution, tool invocation, and operational tasks.
  L3_orchestration: Workflow coordination, error handling, and cross-component routing.
  L4_memory: State management, context retrieval, and history tracking.
  L5_safety: Guardrails, compliance checks, cost management, and ethical boundaries.

phases:
  P1_retrieve: Gathering context, understanding requests, and fetching necessary data.
  P2_inspect: Analyzing data, vetting quality, embedding content, and converting formats.
  P3_aggregate: Refining results, executing tools, updating state, and compiling outputs.
  P4_safety: Final safety validation, cost accounting, and policy enforcement.

intents:
  - check_format
  - check_message_format
  - check_outreach_rules
  - check_prompt_rules
  - check_prompt_structure
  - check_resume_format
  - check_resume_rules
  - check_rules
  - check_schema_rules
  - check_schema_structure
  - check_structure
  - convert
  - convert_content
  - convert_prompt_content
  - convert_schema_content
  - detect_anomalies
  - find_msg_problems
  - find_prompt_problems
  - find_resume_problems
  - find_schema_problems
  - gather_context_inputs
  - get_prompt_info
  - get_schema_info
  - control_resources
  - control_outreach_resources
  - control_prompt_resources
  - control_resume_resources
  - control_schema_resources
  - select_optimal
  - pick_message
  - pick_resume_content
  - sjob_fit
  - sperson
  - update_outreach
  - update_prompt_state
  - update_schema_state
  - sync_status
  - execute_actions
  - execute_prompt_actions
  - execute_schema_actions
  - vectorize_message
  - vectorize_resume

axes:
  - embedding_compare_meaning
  - manage_config_costs_update_memory
  - manage_data_costs_update_memory
  - manage_observability_costs_update_memory
  - manage_prompt_costs_update_memory
  - manage_runtime_costs_update_memory
  - manage_schema_costs_update_memory
  - manage_scripts_costs_update_memory
  - manage_tests_costs_update_memory
  - policy_check_safety
  - refinement_adjust_scores
  - routing_retry_task
  - semantic_adjust_scores
  - understand_request
  - update_memory
  - use_a_tool
  - utility_prepare_information

verb_groups: []

