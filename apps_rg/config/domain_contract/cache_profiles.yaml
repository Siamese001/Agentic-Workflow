# apps_rg cache profile — apps-core-contract-rectification-a8f3c2 Phase 3.2
cache_profile_id: cp::apps_rg::resume_generation::v1
app_id: apps_rg
version: 1.0.0
status: active
source_app_config_ref: apps_rg/config/domain_contract/cache_profiles.yaml
created_at: '2026-05-03'

semantic_cache:
  enabled: true
  namespace: apps_rg.resume_gen.v1
  similarity_threshold: 0.88
  compatibility_check_fields:
    - role_compatible
    - freshness_within_ttl
    - provenance_known
  # W5 (chroma-graphrag-core-wiring-gaps-b3f7a1 RCA decision KEEP_QUARANTINED_DEPRECATED):
  # Generic R1B path is now live via package_driven_l0_binding._read_semantic_cache_profile()
  # + check_d2_semantic_cache(). apps_rg/cache/r1b_adapter.py remains QUARANTINED and
  # DEPRECATED — no app-specific adapter code is required or used.
  # RCA: docs/architecture/rca/RCA_apps_rg_r1b_adapter_L4_import_violation.md
  live_wiring_deferred: false
  wiring_gate: CLEARED_BY_W1_GENERIC_R1B_CACHE_WIRING

ttl_seconds: 3600
cache_scope: user
grounded_only: true
invalidation_triggers:
  - config_version_bump
  - policy_hash_change
