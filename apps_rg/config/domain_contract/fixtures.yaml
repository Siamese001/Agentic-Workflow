# apps_rg golden fixtures — must PASS with the app-domain rubric.
- fixture_id: afix::apps_rg::resume_generation::golden_senior_eng
  app_id: apps_rg
  task_class: resume_generation
  fixture_type: golden
  version: "1.0.0"
  status: active
  input_ref: "tests/fixtures/apps_rg/senior_eng_profile.json"
  expected_disposition: ALLOW
  expected_gate_results:
    factual_grounding: PASS
    role_alignment: PASS
    ats_readability: PASS
    specificity: PASS
    format_compliance: PASS
    no_fabrication: PASS
  expected_output_assertions:
    - "header_block present"
    - "experience_block has >= 3 entries"
    - "skills_block subset of profile.skills"
    - "no employer mentioned that is absent from profile.experiences"
    - "every metric traces to a profile evidence_id"
  source_app_config_ref: "apps_rg/tests/fixtures/"
  created_at: "2026-05-01"

- fixture_id: afix::apps_rg::resume_generation::golden_pm
  app_id: apps_rg
  task_class: resume_generation
  fixture_type: golden
  version: "1.0.0"
  status: active
  input_ref: "tests/fixtures/apps_rg/pm_profile.json"
  expected_disposition: ALLOW
  expected_gate_results:
    factual_grounding: PASS
    role_alignment: PASS
    ats_readability: PASS
    no_fabrication: PASS
    format_compliance: PASS
  expected_output_assertions:
    - "professional_summary mentions product outcomes (supported by profile)"
    - "experience_block titles match profile.experiences exactly"
    - "education_block.entries equal profile.education"
  source_app_config_ref: "apps_rg/tests/fixtures/"
  created_at: "2026-05-01"
