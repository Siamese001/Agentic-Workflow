# Test app U0 validation profile
# Minimal valid profile for resolver testing

schema_version: "1.0"
profile_type: "u0_validation"
app_id: "apps_test"

validation_rules:
  required_payload_fields: ["tenant_id", "capability"]
  max_payload_size_bytes: 1048576
  allowed_content_types: ["application/json"]
  schema_validation_enabled: true
  custom_validators: []

fail_closed_behavior:
  on_missing_field: "reject"
  on_schema_mismatch: "reject"
  on_size_exceeded: "reject"

validation_behavior:
  missing_profile: FAIL_CLOSED
  invalid_profile: FAIL_CLOSED
  unknown_app: FAIL_CLOSED
