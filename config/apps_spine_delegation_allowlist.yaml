# Allowlist for `check_apps_spine_delegation.py` — apps_* packages exempt
# from the spine delegation invariant per ADR-078.
#
# Each entry MUST carry:
#   - package: top-level apps_*/ directory name
#   - reason:  non-empty justification (bare "" rejected)
#   - expires: ISO date; gate fails closed when entry is past expiry
#
# Plan: .windsurf/plans/adg-three-bucket-unified-c4f8e2.md (W3 P3.2)
# Audit log: docs/reports/adg/apps_spine_delegation_gate_report.json

# apps_underwriting_ai allowlist entry removed DS-6 (2026-05-05):
# underwriting_capability_registry.py now imports L0Route from
# agentic_core.L0_routing.types.routing_artifact_types, creating the
# required spine delegation edge. Gate confirmed violation=0 after ADG refresh.
allowed_packages: []
