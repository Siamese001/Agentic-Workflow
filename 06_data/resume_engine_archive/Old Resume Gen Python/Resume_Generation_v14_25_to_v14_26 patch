--- Resume_Generation_v14_25.py	2025-01-01 00:00:00.000000000 +0000
+++ Resume_Generation_v14_26.py	2025-01-01 00:00:01.000000000 +0000
@@ -31,7 +31,7 @@
 from dotenv import load_dotenv
 load_dotenv()
 
-__version__ = "14_25"
+__version__ = "14_26"
 
 try:
     import google.generativeai as genai
@@ -7193,6 +7193,17 @@
             parsed_jd_sim = {"primary_theme": thematic_analysis.primary_theme.get("name"), "secondary_themes": [t.get("name") for t in thematic_analysis.secondary_themes], "required_skills": thematic_analysis.primary_theme.get("keywords", []) + [kw for t in thematic_analysis.secondary_themes for kw in t.get("keywords", [])]}
             self.jd_enforcer.validate_jd_parsing(parsed_jd_sim, "GATE-1")
             self.jd_enforcer.validate_thematic_analysis(thematic_analysis, "GATE-2")
+            
+            # ENHANCED LOGGING: HOP-0 Results
+            self.logger.info(f"  [HOP-0] Signal Quality Score: {thematic_analysis.signal_quality_score:.3f}")
+            self.logger.info(f"  [HOP-0] Retrieval Method: {thematic_analysis.retrieval_method}")
+            self.logger.info(f"  [HOP-0] Primary Theme: {thematic_analysis.primary_theme.get('name', 'N/A')} (confidence: {thematic_analysis.primary_theme.get('confidence', 0):.2f})")
+            self.logger.info(f"  [HOP-0] Primary Keywords: {', '.join(thematic_analysis.primary_theme.get('keywords', [])[:5])}")
+            if thematic_analysis.secondary_themes:
+                sec_themes = ', '.join([t.get('name', 'N/A') for t in thematic_analysis.secondary_themes[:3]])
+                self.logger.info(f"  [HOP-0] Secondary Themes (top 3): {sec_themes}")
+            top_diffs = thematic_analysis.competitive_intelligence.get_top_differentiators(5)
+            self.logger.info(f"  [HOP-0] Top 5 Differentiators: {', '.join(top_diffs)}")
 
             hop_checkpoint = self._create_checkpoint(
                 "HOP-0", "JD Analysis & RAG", [],
@@ -7229,6 +7240,12 @@
             clerk = ClerkExtractor(self.master_resume)
             extracted_data, clerk_validation_results = clerk.extract()
             
+            # ENHANCED LOGGING: HOP-1 Results
+            bullet_count = sum(len(exp.get('bullets', [])) for exp in extracted_data.get('experience_sections', []))
+            self.logger.info(f"  [HOP-1] Bullets Extracted: {bullet_count}")
+            hallucination_issues = [vr for vr in clerk_validation_results if 'HALLUCINATION' in vr.rule_id and not vr.passed]
+            self.logger.info(f"  [HOP-1] Hallucination Issues: {len(hallucination_issues)}")
+            
             hop_checkpoint = self._create_checkpoint(
                 "HOP-1", "Clerk Extraction", clerk_validation_results,
                 {"bullets_extracted": sum(len(exp.get('bullets', [])) for exp in extracted_data.get('experience_sections', []))},
@@ -7264,6 +7281,13 @@
             enricher = DataEnricher(self.master_resume, extracted_data, thematic_analysis)
             enriched_scaffold, enrichment_validation_results = enricher.enrich()
             
+            # ENHANCED LOGGING: HOP-2 Results
+            self.logger.info(f"  [HOP-2] Enrichment Complete")
+            dup_issues = [vr for vr in enrichment_validation_results if 'DUPLICATE' in vr.rule_id and not vr.passed]
+            self.logger.info(f"  [HOP-2] Duplicate Detection Issues: {len(dup_issues)}")
+            if enriched_scaffold.get('experience_sections'):
+                self.logger.info(f"  [HOP-2] Experience Sections Enriched: {len(enriched_scaffold['experience_sections'])}")
+            
             hop_checkpoint = self._create_checkpoint(
                 "HOP-2", "Scaffold Enrichment", enrichment_validation_results,
                 enriched_scaffold, start_time=hop_start_time
@@ -7379,6 +7403,25 @@
                 attempt_results, overall_attempt_status, failed_sections = validator.validate(attempt_buffer, thematic_analysis, job_description)
                 total_validation_runs += 1
                 
+                # ENHANCED LOGGING: HOP-3 Retry Attempt Details
+                self.logger.info(f"  [HOP-3] Attempt {attempt_num}/{self.HOP3_MAX_ATTEMPTS} Validation:")
+                self.logger.info(f"    Overall Status: {'PASS' if overall_attempt_status else 'FAIL'}")
+                if not overall_attempt_status:
+                    failed_rules = [vr for vr in attempt_results if not vr.passed and vr.severity in [ValidationSeverity.CRITICAL, ValidationSeverity.HIGH]]
+                    self.logger.info(f"    Failed Sections: {', '.join([s.name for s in failed_sections])}")
+                    self.logger.info(f"    Critical/High Failures: {len(failed_rules)}")
+                    for vr in failed_rules[:5]:  # Log first 5 failures
+                        # Extract value vs target from message if available
+                        if '(' in vr.message and ')' in vr.message:
+                            value_target = vr.message[vr.message.find('('):vr.message.find(')')+1]
+                        else:
+                            value_target = ""
+                        self.logger.info(f"      - {vr.rule_id}: {value_target}")
+                else:
+                    self.logger.info(f"    ✓ All validations passed")
+                    
+                # Log current temperatures for this attempt
+                self.logger.debug(f"    Temperatures used: {current_temperatures}")
+                
                 if overall_attempt_status:
                     # Validation passed! Break out of retry loop.
                     self.logger.info(f"  ✓ [HOP-3] Content validation PASSED on attempt {attempt_num}.")
@@ -7444,6 +7487,11 @@
                 )
             self.hop_checkpoints.append(final_checkpoint)
             self._check_hop_status(final_checkpoint, allow_warnings=True)
+            
+            # ENHANCED LOGGING: HOP-3 Final Summary
+            self.logger.info(f"  [HOP-3] Final Locked Temperatures: {final_locked_temps}")
+            self.logger.info(f"  [HOP-3] Total Validation Runs: {total_validation_runs}")
+            self.logger.info(f"  [HOP-3] Total API Calls: {total_api_calls}")
             
             return merged_output, total_api_calls
             
@@ -7541,6 +7589,12 @@
             staging_buffer = ImmutableStagingBuffer(sanitized_buffer_data)
             staging_buffer.lock()
             
+            # ENHANCED LOGGING: HOP-4.5 Sanitization Results
+            self.logger.info(f"  [HOP-4.5] Text Sanitization Complete")
+            self.logger.info(f"    Issues Detected: {len(sanitization_results)}")
+            self.logger.info(f"    Buffer Locked: {staging_buffer.is_locked()}")
+            self.logger.info(f"    Buffer Sections: {len(staging_buffer.data)}")
+            
             hop_checkpoint = self._create_checkpoint(
                 "HOP-4.5", "Staging & Sanitization", sanitization_results,
                 None, start_time=hop_start_time
@@ -7613,6 +7667,30 @@
             validator = PreFlightValidator(self.master_resume)
             validation_results, overall_status, failed_sections = validator.validate(staging_buffer, thematic_analysis, job_description)
             self.validation_results = validation_results
+            
+            # ENHANCED LOGGING: HOP-5 Key Validation Results
+            self.logger.info(f"  [HOP-5] Validation Complete - Overall: {'PASS' if overall_status else 'FAIL'}")
+            
+            # Log critical validation metrics
+            critical_rules = {
+                'VG_TOTAL_WORD_COUNT': 'Total Word Count',
+                'VG_HEADLINE_WORD_COUNT': 'Headline Word Count',
+                'VG_SENTENCE_COUNT_K1': 'Exec Summary Sentences',
+                'WORD_DISTRIBUTION_UNIFY_IBM': 'Unify/IBM Distribution',
+                'UNIFY_IBM_RATIO': 'Unify/IBM Ratio',
+                'BUFFER_LOCK_STATUS': 'Buffer Lock Status',
+                'SIGNAL_QUALITY_GATE': 'Signal Quality'
+            }
+            
+            for rule_id, rule_name in critical_rules.items():
+                result = next((vr for vr in validation_results if vr.rule_id == rule_id), None)
+                if result:
+                    status = "✓ PASS" if result.passed else "✗ FAIL"
+                    self.logger.info(f"    [{rule_id}] {rule_name}: {status}")
+                    if not result.passed and result.message:
+                        # Extract and log the actual vs target values
+                        self.logger.info(f"      {result.message}")
+            
             hop_checkpoint = self._create_checkpoint(
                 "HOP-5", "Pre-Flight Validation", validation_results,
                 {"all_rules_checked": len(validator.engine.rules), "all_passed": overall_status},
@@ -7665,6 +7743,10 @@
             engine = GateDecisionEngine()
             decision, reason = engine.decide(hop5_results)
             
+            # ENHANCED LOGGING: HOP-6 Gate Decision
+            self.logger.info(f"  [HOP-6] Gate Decision: {decision.value}")
+            self.logger.info(f"  [HOP-6] Reason: {reason}")
+            
             hop_checkpoint = self._create_checkpoint(
                 "HOP-6", "Gate Decision", [],
                 {"decision": decision.value, "reason": reason},
@@ -7736,6 +7818,15 @@
                 )
             )
             
+            # ENHANCED LOGGING: HOP-7 Rendering Results
+            self.logger.info(f"  [HOP-7] Files Generated: {len(file_paths)}")
+            for file_type, path in file_paths.items():
+                file_size = os.path.getsize(path) if os.path.exists(path) else 0
+                self.logger.info(f"    {file_type}: {os.path.basename(path)} ({file_size} bytes)")
+            
+            app_tracker_issues = [vr for vr in app_tracker_validation if not vr.passed]
+            self.logger.info(f"  [HOP-7] App Tracker Validation: {len(app_tracker_issues)} issues")
+            
             return file_paths, file_contents
             
         except Exception as e:
@@ -7778,6 +7869,10 @@
             qa_generation_validation_results, qa_report_text = qa_generator.generate(staging_buffer, thematic_analysis, validation_results, file_contents)
             self.validation_results.extend(qa_generation_validation_results)
             
+            # ENHANCED LOGGING: HOP-8 QA Report Generation
+            qa_success = all(vr.passed for vr in qa_generation_validation_results)
+            self.logger.info(f"  [HOP-8] QA Report Generation: {'SUCCESS' if qa_success else 'FAILED'}")
+            
             hop_checkpoint = self._create_checkpoint(
                 "HOP-8", "QA Report Generation",
                 qa_generation_validation_results,
@@ -8376,18 +8471,16 @@
 class QAReportGenerator:
-    """Generates the QA report by building individual sections (v14.11 structure)."""
+    """Generates a simplified QA report focusing on essential summaries (v14.26)."""
 
     QA_REPORT_SECTIONS = [
-        {"method": "_build_qa_section_1_signal_quality", "args": ["staging_buffer", "thematic_analysis", "validation_results"]},
-        {"method": "_build_qa_section_2_signal_flow_map", "args": ["staging_buffer", "thematic_analysis"]},
+        {"method": "_build_qa_section_1_signal_quality_summary", "args": ["thematic_analysis", "validation_results"]},
         {"method": "_build_qa_section_3_hop_summary", "args": []},
-        {"method": "_build_qa_section_4_word_count_distribution", "args": ["validation_results"]},
-        {"method": "_build_qa_section_5_provenance", "args": ["staging_buffer"]},
-        {"method": "_build_qa_section_6_content_authenticity", "args": ["validation_results"]}, # Specific method
+        {"method": "_build_qa_section_5_critical_failures", "args": ["validation_results"]},
         {"method": "_build_qa_section_7_prod_readiness", "args": ["validation_results"]},
-        {"method": "_build_qa_section_8_similarity_summary", "args": ["validation_results"]}, # Renamed & Updated
-        {"method": "_build_qa_section_9_structural_validation", "args": ["validation_results"]}, # Specific method
         {"method": "_build_qa_section_10_jd_enforcement", "args": []},
-        {"method": "_build_qa_section_11_final_format", "args": ["validation_results", "file_contents"]},
     ]
 
     def __init__(self, orchestrator: 'WorkflowOrchestrator'):
@@ -8405,7 +8498,7 @@
         """Generates the full QA report."""
         qa_generation_validation_results = []
         report_lines = [
-            f"RESUME QA REPORT (Engine: v{__version__}, Arch: Job_Workflow_v14.14_Refined)", # Updated Arch Version
+            f"RESUME QA REPORT - SIMPLIFIED SUMMARY (Engine: v{__version__})",
             f"Generated: {datetime.now().isoformat()}",
+            "",
+            "NOTE: Detailed metrics are logged during workflow execution. This report provides high-level summaries.",
+            ""
         ]
         self.logger.info("  Building QA Report Sections...")
 
@@ -8471,140 +8564,93 @@
         ))
         return qa_generation_validation_results, qa_report_text
 
-    def _build_qa_section_1_signal_quality(
+    def _build_qa_section_1_signal_quality_summary(
         self,
-        staging_buffer: ImmutableStagingBuffer,
         thematic_analysis: ThematicAnalysis,
         validation_results: List[ValidationResult]
     ) -> List[str]:
-        lines = ["", "**1. SIGNAL QUALITY & TARGET ALIGNMENT**", ""]
-        lines.append("Summarizes RAG analysis quality and the alignment of generated content to weighted JD signals (Primary/Secondary Themes, Differentiators, Narrative, Role Keywords) using context-aware raw scores.")
+        """Simplified signal quality summary showing only overall scores."""
+        lines = ["", "**1. SIGNAL QUALITY SUMMARY**", ""]
         lines.append("")
+        
         rag_quality = getattr(thematic_analysis, 'signal_quality_score', 0.0)
-        rag_display = f"{rag_quality:.1%}" # As percentage
-        rag_result = next((vr for vr in validation_results if vr.rule_id == "SIGNAL_QUALITY_GATE"), None)
-        rag_status_icon = "PASS" if rag_result and rag_result.passed else "FAIL"
-
-        # --- Extract Section Signal Results & Scores from SECTION_SIGNAL_COMPLIANCE validation results ---
-        section_signals_data = []
-        for section_config in self.orchestrator.SECTION_SIGNAL_TARGETS_CONFIG:
-            section_id, targets, _ = section_config
-            section_name = section_id.name # e.g., 'K1_EXECUTIVE_SUMMARY'
-            rule_id_for_section = f"SECTION_SIGNAL_COMPLIANCE_{section_name}"
-            signal_result = next((vr for vr in validation_results if vr.rule_id == rule_id_for_section), None)
-
-            if signal_result:
-                raw_score = 0.0
-                status_icon = "PASS" if signal_result.passed else "FAIL"
-                # Extract raw score from message (e.g., "Section K1... Raw Score: 54.2 (Target: 47.5-65.0)")
-                score_match = re.search(r"Raw Score:\s*([\d.]+)\s*\(Target:\s*([\d.]+)-([\d.]+)\)", signal_result.message)
-                if score_match:
-                    raw_score = float(score_match.group(1))
-                    target_min = float(score_match.group(2))
-                    target_max = float(score_match.group(3))
-                    target_range_str = f"{target_min:.1f}-{target_max:.1f}"
-                else:
-                    target_range_str = "N/A"
-
-                section_signals_data.append({
-                    'section': section_name,
-                    'raw_score': raw_score,
-                    'target_range': target_range_str,
-                    'status': status_icon
-                })
-            else:
-                # If validation result not found for this section, mark N/A
-                section_signals_data.append({
-                    'section': section_name,
-                    'raw_score': 0.0,
-                    'target_range': 'N/A',
-                    'status': 'N/A'
-                })
-
-        # --- ASCII Bar Chart Display (v14.11 style) ---
-        lines.append("```markdown")
-        lines.append(f"RAG Quality Score: {rag_display} (Target: ≥75%)  [{rag_status_icon}]")
-        lines.append("")
-        lines.append("Section Signal Alignment (Raw Scores vs. Targets):")
-        lines.append("─" * 70)
-
-        max_bar_width = 50
-        max_raw_score_for_scale = 80.0 # Scale bars assuming max possible score ~80
-
-        for item in section_signals_data:
-            section_short = item['section'].replace('_', ' ')
-            raw_score = item['raw_score']
-            target_range = item['target_range']
-            status = item['status']
-
-            # Create bar
-            bar_length = int((raw_score / max_raw_score_for_scale) * max_bar_width)
-            bar = "█" * bar_length
-
-            # Format line
-            section_padded = f"{section_short:<28}"
-            score_display = f"{raw_score:>5.1f}"
-            target_display = f"(Tgt:{target_range})"
-            status_display = f"[{status}]"
-            line = f"{section_padded} {bar} {score_display} {target_display:>18} {status_display}"
-            lines.append(line)
-
-        lines.append("─" * 70)
-        lines.append("```")
+        retrieval_method = getattr(thematic_analysis, 'retrieval_method', 'N/A')
+        
+        lines.append(f"RAG Quality Score: {rag_quality:.1%}")
+        lines.append(f"Retrieval Method: {retrieval_method}")
+        
+        rag_result = next((vr for vr in validation_results if vr.rule_id == "SIGNAL_QUALITY_GATE"), None)
+        if rag_result:
+            status = "✓ PASS" if rag_result.passed else "✗ FAIL"
+            lines.append(f"Signal Quality Gate: {status}")
+        
+        # Get primary theme info
+        primary_theme = thematic_analysis.primary_theme.get('name', 'N/A')
+        lines.append(f"Primary Theme: {primary_theme}")
+        
         return lines
 
-    def _build_qa_section_2_signal_flow_map(
-        self,
-        staging_buffer: ImmutableStagingBuffer,
-        thematic_analysis: ThematicAnalysis
-    ) -> List[str]:
-        """Build QA Section 2: HOP-0 RAG Signal Flow Map (Simplified v14.11)."""
-        lines = ["", "**2. HOP-0 RAG SIGNAL FLOW MAP**", ""]
-        lines.append("Traces signal weights from RAG to section targets. Differentiator weights guide K1/K13 content. Thematic keywords inform K5-K10 bullets. Authenticity patterns enforce stylistic consistency.")
+    def _build_qa_section_5_critical_failures(
+        self, validation_results: List[ValidationResult]
+    ) -> List[str]:
+        """Summarizes critical and high severity validation failures."""
+        lines = ["", "**2. CRITICAL & HIGH SEVERITY FAILURES**", ""]
         lines.append("")
-        lines.append("```markdown")
-
-        # Extract top differentiators (used in K1, K13, etc.)
-        top_diffs = thematic_analysis.competitive_intelligence.get_top_differentiators(5)
-        lines.append(f"Top 5 Differentiators (K1/K13 Focus): {', '.join(top_diffs)}")
-
-        # Primary Theme Keywords (used in K5-K10)
-        primary_keywords = thematic_analysis.primary_theme.get('keywords', [])[:5]
-        lines.append(f"Primary Theme Keywords (K5-K10): {', '.join(primary_keywords)}")
-
-        # Problem/Solution Narratives
-        narratives = thematic_analysis.problem_solution_narratives
-        if narratives:
-            problems = narratives.get('common_problems', [])[:2]
-            solutions = narratives.get('solution_patterns', [])[:2]
-            lines.append(f"Problem Patterns (K1/K13): {', '.join(problems)}")
-            lines.append(f"Solution Patterns (K1/K13): {', '.join(solutions)}")
-        else:
-            lines.append("Problem/Solution Narratives: [Not Available]")
-
-        # Authenticity Patterns (Example: Verbs, Competency Phrasing)
-        auth_patterns = thematic_analysis.authenticity_patterns.get('patterns', {})
-        verbs = auth_patterns.get('achievement_verb_patterns', [])[:3]
-        comp_phrasing = auth_patterns.get('competency_phrasing', [])[:2]
-        lines.append(f"Canonical Verbs (K5-K10): {', '.join(verbs)}")
-        lines.append(f"Competency Phrasing (K10): {', '.join(comp_phrasing) if comp_phrasing else '[None]'}")
-
-        lines.append("```")
+        
+        critical_failures = [
+            vr for vr in validation_results 
+            if not vr.passed and vr.severity in [ValidationSeverity.CRITICAL, ValidationSeverity.HIGH]
+        ]
+        
+        if not critical_failures:
+            lines.append("✓ No critical or high severity failures detected.")
+        else:
+            lines.append(f"Found {len(critical_failures)} critical/high severity failures:")
+            lines.append("")
+            
+            # Group by severity
+            critical = [vr for vr in critical_failures if vr.severity == ValidationSeverity.CRITICAL]
+            high = [vr for vr in critical_failures if vr.severity == ValidationSeverity.HIGH]
+            
+            if critical:
+                lines.append("CRITICAL Failures:")
+                for vr in critical[:10]:  # Limit to 10
+                    lines.append(f"  • {vr.rule_id}: {vr.message[:100]}...")
+                if len(critical) > 10:
+                    lines.append(f"  ... and {len(critical) - 10} more")
+                lines.append("")
+            
+            if high:
+                lines.append("HIGH Severity Failures:")
+                for vr in high[:10]:  # Limit to 10
+                    lines.append(f"  • {vr.rule_id}: {vr.message[:100]}...")
+                if len(high) > 10:
+                    lines.append(f"  ... and {len(high) - 10} more")
+        
         return lines
 
     def _build_qa_section_3_hop_summary(self) -> List[str]:
-        """Build QA Section 3: Hop-by-Hop Execution Summary (v14.11 table format)."""
-        lines = ["", "**3. HOP-BY-HOP EXECUTION SUMMARY**", ""]
-        lines.append("Provides a concise overview of each workflow hop's execution status, output, duration, and metadata (e.g., API calls, validations run).")
+        """Build simplified hop execution summary."""
+        lines = ["", "**3. WORKFLOW HOP SUMMARY**", ""]
         lines.append("")
-        lines.append("```markdown")
+        
+        if not self.orchestrator.hop_checkpoints:
+            lines.append("No hop checkpoints recorded.")
+            return lines
+        
         lines.append("Hop ID   Hop Name                          Status   Duration   API Calls   Output Summary")
         lines.append("------   --------------------------------  ------   --------   ---------   --------------")
 
         for chk in self.orchestrator.hop_checkpoints:
             hop_id_str = chk.hop_id
             hop_name = chk.hop_name[:32].ljust(32)
             status = chk.status.value[:6].ljust(6)
+            
             duration = chk.duration
             duration_str = f"{duration:.2f}s" if duration is not None else "N/A"
-            duration_str = duration_str.rjust(8) # Right align
+            duration_str = duration_str.rjust(8)
+            
             api_calls_meta = chk.metadata.get('gemini_api_calls', 0)
             api_calls_str = str(api_calls_meta).rjust(9)
 
@@ -8631,206 +8677,18 @@
                 output_summary = "N/A"
                 if chk.output_data:
                     if isinstance(chk.output_data, dict):
-                        keys_str = ', '.join(list(chk.output_data.keys())[:3]) # Show first 3 keys
+                        keys_str = ', '.join(list(chk.output_data.keys())[:2])
                         output_summary = f"Dict({keys_str}...)" if len(chk.output_data) > 3 else f"Dict({keys_str})"
                     elif isinstance(chk.output_data, str):
                         output_summary = chk.output_data[:20] + "..." if len(chk.output_data) > 20 else chk.output_data
@@ -8631,206 +8677,18 @@
             line = f"{hop_id_str}   {hop_name}  {status}   {duration_str}   {api_calls_str}   {output_summary}"
             lines.append(line)
 
-        lines.append("```")
-        return lines
-
-    def _build_qa_section_4_word_count_distribution(
-        self, validation_results: List[ValidationResult]
-    ) -> List[str]:
-        """Build QA Section 4: Word Count & Distribution Compliance (v14.11 style table)."""
-        lines = ["", "**4. WORD COUNT & DISTRIBUTION COMPLIANCE**", ""]
-        lines.append("Validates total resume word count, distribution across key sections (Unify/IBM), and their relative ratio to ensure balanced content focus.")
-        lines.append("")
-        lines.append("```markdown")
-
-        # Extract relevant validation results
-        total_wc_result = next((vr for vr in validation_results if vr.rule_id == "VG_TOTAL_WORD_COUNT"), None)
-        dist_result = next((vr for vr in validation_results if vr.rule_id == "WORD_DISTRIBUTION_UNIFY_IBM"), None)
-        ratio_result = next((vr for vr in validation_results if vr.rule_id == "UNIFY_IBM_RATIO"), None)
-
-        lines.append("Metric                     Value        Target           Status")
-        lines.append("--------------------------  -----------  ---------------  ------")
-
-        # Total Word Count
-        if total_wc_result:
-            match = re.search(r"Total word count: (\d+) \(Target: (\d+)-(\d+)\)", total_wc_result.message)
-            if match:
-                value = match.group(1)
-                target = f"{match.group(2)}-{match.group(3)}"
-                status = "PASS" if total_wc_result.passed else "FAIL"
-                lines.append(f"Total Word Count            {value:<11}  {target:<15}  {status}")
-            else:
-                lines.append(f"Total Word Count            N/A          N/A              N/A")
-        else:
-             lines.append(f"Total Word Count            N/A          N/A              N/A")
-
-        # Unify/IBM Distribution
-        if dist_result:
-            match = re.search(r"Unify\+IBM: ([\d.]+)% \(Target: ([\d.]+)%-([\d.]+)%\)", dist_result.message)
-            if match:
-                value = f"{match.group(1)}%"
-                target = f"{match.group(2)}%-{match.group(3)}%"
-                status = "PASS" if dist_result.passed else "FAIL"
-                lines.append(f"Unify/IBM Distribution %    {value:<11}  {target:<15}  {status}")
-            else:
-                 lines.append(f"Unify/IBM Distribution %    N/A          N/A              N/A")
-        else:
-            lines.append(f"Unify/IBM Distribution %    N/A          N/A              N/A")
-
-        # Unify/IBM Ratio
-        if ratio_result:
-            match = re.search(r"Unify/IBM Ratio: ([\d.]+) \(Target: ([\d.]+)-([\d.]+)\)", ratio_result.message)
-            if match:
-                value = match.group(1)
-                target = f"{match.group(2)}-{match.group(3)}"
-                status = "PASS" if ratio_result.passed else "FAIL"
-                lines.append(f"Unify/IBM Ratio             {value:<11}  {target:<15}  {status}")
-            else:
-                 lines.append(f"Unify/IBM Ratio             N/A          N/A              N/A")
-        else:
-             lines.append(f"Unify/IBM Ratio             N/A          N/A              N/A")
-
-        lines.append("```")
-        return lines
-
-    def _build_qa_section_5_provenance(
-        self, staging_buffer: ImmutableStagingBuffer
-    ) -> List[str]:
-        """Build QA Section 5: Bullet Provenance & Word Count (v14.11 style table)."""
-        lines = ["", "**5. BULLET PROVENANCE & WORD COUNT**", ""]
-        lines.append("Displays the distribution of bullet provenance types (Verbatim, Customized, Synthetic) and validates word count for each bullet against targets (18-28 words). Removed the problematic average word count comparison.")
-        lines.append("")
-        lines.append("```markdown")
-
-        # Access buffer data for provenance & word count
-        section_configs = self.orchestrator.SECTION_SIGNAL_TARGETS_CONFIG
-
-        # Gather provenance and word count data from buffer
-        provenance_counts = {'Verbatim': 0, 'Customized': 0, 'Synthetic': 0, 'Other': 0}
-        word_counts = []
-
-        for section_config in section_configs:
-            section_id, _, _ = section_config
-            section_data = staging_buffer.get(section_id.value)
-            if isinstance(section_data, list): # If list of bullets
-                for bullet_dict in section_data:
-                    if isinstance(bullet_dict, dict):
-                        prov = bullet_dict.get('provenance', 'Other')
-                        if prov in provenance_counts:
-                            provenance_counts[prov] += 1
-                        else:
-                            provenance_counts['Other'] += 1
-                        wc = bullet_dict.get('word_count', 0)
-                        if wc > 0: # Only count if word count was set
-                            word_counts.append(wc)
-
-        total_bullets = sum(provenance_counts.values())
-
-        lines.append("Provenance Type   Count   Percentage")
-        lines.append("----------------  ------  ----------")
-        for prov_type, count in provenance_counts.items():
-            if total_bullets > 0:
-                pct = (count / total_bullets) * 100
-            else:
-                pct = 0.0
-            lines.append(f"{prov_type:<16}  {count:>6}  {pct:>9.1f}%")
-
-        lines.append("")
-        lines.append(f"Total Bullets: {total_bullets}")
-
-        if word_counts:
-            avg_wc = sum(word_counts) / len(word_counts)
-            min_wc = min(word_counts)
-            max_wc = max(word_counts)
-            lines.append("")
-            lines.append("Word Count Statistics (Bullets Only):")
-            lines.append(f"  Average:  {avg_wc:.1f} words")
-            lines.append(f"  Min:      {min_wc} words")
-            lines.append(f"  Max:      {max_wc} words")
-            lines.append(f"  Target Range: 18-28 words per bullet")
-        else:
-            lines.append("")
-            lines.append("Word Count Statistics: N/A (No bullets with word_count found)")
-
-        lines.append("```")
-        return lines
-
-    def _build_qa_section_6_content_authenticity(
-        self, validation_results: List[ValidationResult]
-    ) -> List[str]:
-        """Build QA Section 6: Content Authenticity (v14.11 style)."""
-        lines = ["", "**6. CONTENT AUTHENTICITY**", ""]
-        lines.append("Verifies generated content against authenticity checks (placeholders, forbidden phrases, hallucination, etc.). Ensures professional tone and accuracy.")
-        lines.append("")
-        lines.append("```markdown")
-
-        authenticity_rule_ids = [
-            "CONTENT_NO_PLACEHOLDERS",
-            "CONTENT_NO_FORBIDDEN_PHRASES",
-            "HALLUCINATION_DETECTION",
-            "NARRATIVE_MINING_PRESENCE"
-        ]
-
-        lines.append("Check                         Status   Message")
-        lines.append("----------------------------  ------   -------")
-
-        for rule_id in authenticity_rule_ids:
-            result = next((vr for vr in validation_results if vr.rule_id == rule_id), None)
-            if result:
-                status = "PASS" if result.passed else "FAIL"
-                message_short = result.message[:50] + "..." if len(result.message) > 50 else result.message
-                message_short = message_short.replace("\n", " ") # Remove newlines
-                lines.append(f"{rule_id:<28}  {status:<6}   {message_short}")
-            else:
-                lines.append(f"{rule_id:<28}  N/A      Rule result not found")
-
-        lines.append("```")
         return lines
 
     def _build_qa_section_7_prod_readiness(
         self, validation_results: List[ValidationResult]
     ) -> List[str]:
-        """Build QA Section 7: Production Readiness (v14.11 style)."""
-        lines = ["", "**7. PRODUCTION READINESS**", ""]
-        lines.append("Aggregated view of critical, high, and medium severity failures. Indicates overall production-readiness status.")
+        """Build production readiness summary."""
+        lines = ["", "**4. PRODUCTION READINESS**", ""]
         lines.append("")
-        lines.append("```markdown")
 
         severity_counts = {'CRITICAL': 0, 'HIGH': 0, 'MEDIUM': 0, 'LOW': 0, 'INFO': 0}
         failed_by_severity = {'CRITICAL': [], 'HIGH': [], 'MEDIUM': [], 'LOW': [], 'INFO': []}
@@ -8845,39 +8703,21 @@
                     failed_by_severity[sev_str].append(vr.rule_id)
 
-        lines.append("Severity     Failed Checks   Example Failures")
-        lines.append("-----------  --------------  -----------------")
+        lines.append("SEVERITY     FAILED CHECKS")
+        lines.append("-----------  --------------")
         for sev in ['CRITICAL', 'HIGH', 'MEDIUM', 'LOW']:
             count = severity_counts[sev]
-            examples = ', '.join(failed_by_severity[sev][:2]) if failed_by_severity[sev] else "None"
-            lines.append(f"{sev:<11}  {count:>14}   {examples}")
+            lines.append(f"{sev:<11}  {count:>14}")
+
+        lines.append("")
 
-        lines.append("")
         prod_ready = (severity_counts['CRITICAL'] == 0 and severity_counts['HIGH'] == 0)
         readiness_status = "✓ READY" if prod_ready else "✗ NOT READY (Critical/High failures present)"
         lines.append(f"Production Readiness: {readiness_status}")
-        lines.append("```")
-        return lines
-
-    def _build_qa_section_8_similarity_summary(
-        self, validation_results: List[ValidationResult]
-    ) -> List[str]:
-        """Build QA Section 8: Similarity Summary (v14.11 style)."""
-        lines = ["", "**8. SIMILARITY SUMMARY**", ""]
-        lines.append("Aggregates results from similarity checks (Executive Summary vs. sections, pairwise bullets, cover letter vs. JD).")
-        lines.append("")
-        lines.append("```markdown")
 
-        similarity_checks = ["EXEC_SUMMARY_SIMILARITY", "BULLET_PAIRWISE_SIMILARITY", "VG_COVER_LETTER_RELEVANCE_RANGE"]
-        lines.append("Check                         Status   Details")
-        lines.append("----------------------------  ------   -------")
-
-        for rule_id_pattern in similarity_checks:
-            result = next((vr for vr in validation_results if rule_id_pattern in vr.rule_id), None)
-            if result:
-                status = "PASS" if result.passed else "FAIL"
-                details_short = result.message[:60].replace("\n", " ")
-                lines.append(f"{result.rule_id[:28]:<28}  {status:<6}   {details_short}")
-            else:
-                lines.append(f"{rule_id_pattern:<28}  N/A      No result found")
-
-        lines.append("```")
+        if not prod_ready:
+            lines.append("")
+            lines.append("Review Critical/High failures in Section 2 above for details.")
+        
         return lines
 
     def _build_qa_section_9_structural_validation(
@@ -8905,10 +8745,10 @@
     def _build_qa_section_10_jd_enforcement(self) -> List[str]:
-        """Build QA Section 10: JD Enforcement Validation (v14.11 style)."""
-        lines = ["", "**10. JD ENFORCEMENT VALIDATION**", ""]
-        lines.append("Summarizes compliance with JD enforcer gates (GATE-0 through GATE-6), ensuring input quality, parsing, thematic analysis, and content alignment.")
+        """Build JD enforcement summary."""
+        lines = ["", "**5. JD ENFORCEMENT SUMMARY**", ""]
         lines.append("")
-        lines.append("```markdown")
+        
         enforcer = self.orchestrator.jd_enforcer
 
         # Display enforcer gate results stored in checkpoints/validation results
@@ -8918,7 +8758,7 @@
         # Extract gate validation results from checkpoints or validation results
         gate_results = []
+        
         # Check HOP-0 checkpoint for GATE-0, GATE-1, GATE-2
         hop0_chk = next((chk for chk in self.orchestrator.hop_checkpoints if chk.hop_id == "HOP-0"), None)
         if hop0_chk and hop0_chk.validation_results:
@@ -8943,8 +8783,7 @@
         if gate_results:
-            lines.append("Gate       Status   Description")
-            lines.append("---------  ------   -----------")
+            lines.append("GATE       STATUS   DESCRIPTION")
+            lines.append("---------  ------   -----------")
             for gate_id, status, description in gate_results:
                 desc_short = description[:60].replace("\n", " ")
                 lines.append(f"{gate_id:<9}  {status:<6}   {desc_short}")
@@ -8952,104 +8791,8 @@
             lines.append("No JD Enforcement gate results found in checkpoints.")
 
-        lines.append("```")
-        return lines
-
-    def _build_qa_section_11_final_format(
-        self,
-        validation_results: List[ValidationResult],
-        file_contents: Dict[str, str]
-    ) -> List[str]:
-        """Build QA Section 11: Formatting of Outputs (v14.11 pre-formatted style)."""
-        lines = ["", "**11. FORMATTING OF OUTPUTS**", ""]
-        lines.append("Validates the final formatting of the resume, cover letter, and skills list using pre-formatted text (not Markdown tables).")
-        lines.append("")
-        lines.append("```markdown")
-
-        # Define formatting rule IDs to check
-        formatting_rule_ids = [
-            "VG_RESUME_HEADER_H2",
-            "VG_EDU_CERTS_FORMAT",
-            "VG_EXPERIENCE_BULLET_STYLE",
-            "VG_COMPETENCIES_FORMATTING",
-            "VG_EXPERIENCE_RENDER_FORMAT",
-            "VG_COVER_LETTER_STRUCTURE",
-            "VG_COVER_LETTER_SIGNATURE_VALID",
-            "VG_SKILLS_LIST_FORMAT"
-        ]
-
-        # Artifact type mapping (for display purposes)
-        artifact_map = {
-            "VG_RESUME_HEADER_H2": "Resume",
-            "VG_EDU_CERTS_FORMAT": "Resume",
-            "VG_EXPERIENCE_BULLET_STYLE": "Resume",
-            "VG_COMPETENCIES_FORMATTING": "Resume",
-            "VG_EXPERIENCE_RENDER_FORMAT": "Resume",
-            "VG_COVER_LETTER_STRUCTURE": "Cover Letter",
-            "VG_COVER_LETTER_SIGNATURE_VALID": "Cover Letter",
-            "VG_SKILLS_LIST_FORMAT": "Skills List"
-        }
-
-        # Build pre-formatted table (using spaces, not pipes)
-        lines.append("Artifact      Check                            Status")
-        lines.append("------------  -------------------------------  ------")
-
-        for rule_id in formatting_rule_ids:
-            result = next((vr for vr in validation_results if vr.rule_id == rule_id), None)
-            artifact = artifact_map.get(rule_id, "Unknown")
-            status = "PASS" if result and result.passed else "FAIL" if result else "N/A"
-
-            # Format line with fixed width columns (no pipes)
-            artifact_col = f"{artifact:<12}"
-            check_col = f"{rule_id:<31}"
-            status_col = f"{status:<6}"
-            line = f"{artifact_col}  {check_col}  {status_col}"
-            lines.append(line)
-
-        lines.append("```")
         return lines
 
-    def _build_qa_section_9_structural_validation(
-        self, validation_results: List[ValidationResult]
-    ) -> List[str]:
-        """Build QA Section 9: Structural Validation (v14.11 style)."""
-        lines = ["", "**9. STRUCTURAL VALIDATION**", ""]
-        lines.append("Validates the structural presence and integrity of all expected resume sections using STRUCTURE_* rules.")
-        lines.append("")
-        lines.append("```markdown")
-
-        # Extract all STRUCTURE_* validation results
-        structure_results = [vr for vr in validation_results if vr.rule_id.startswith("STRUCTURE_")]
-
-        if structure_results:
-            lines.append("Section                         Status   Message")
-            lines.append("------------------------------  ------   -------")
-
-            for vr in structure_results:
-                # Extract section name from rule_id (e.g., STRUCTURE_K0_HEADLINE_PRESENT -> K0_HEADLINE)
-                section_name = vr.rule_id.replace("STRUCTURE_", "").replace("_PRESENT", "")
-                status = "PASS" if vr.passed else "FAIL"
-                message_short = vr.message[:50].replace("\n", " ")
-                lines.append(f"{section_name:<30}  {status:<6}   {message_short}")
-        else:
-            lines.append("No structural validation results found.")
-
-        lines.append("")
-
-        # Overall structural integrity check
-        all_structure_passed = all(vr.passed for vr in structure_results)
-        structure_status = "✓ All sections present and valid" if all_structure_passed else "✗ Some sections missing or invalid"
-        lines.append(f"Overall Structural Integrity: {structure_status}")
-        lines.append("```")
-        return lines
-
     def _validate_qa_report_formatting(self, qa_report_text: str) -> ValidationResult:
         """Validates that QA report sections use correct formatting (v14.11)."""
         try:
