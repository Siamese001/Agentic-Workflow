Module(
  body=[
    ClassDef(
      name='Redis',
      body=[
        Expr(
          value=Constant(value='Minimal Redis stub so the core module can be imported in tests.')),
        FunctionDef(
          name='__init__',
          args=arguments(
            args=[
              arg(arg='self')],
            vararg=arg(arg='args'),
            kwarg=arg(arg='kwargs')),
          body=[
            Pass()])])])